# doc/aineo.txt: two packets' section-fenced edits merge clean (2026-09-25, dev 9af91a6)
# base = doc/aineo.txt; a = one line inserted two lines under *aineo-report*;
# c = one line under *aineo-commands* and one under *aineo-keys*
$ git merge-file -p a.txt base.txt c.txt > merged.txt; echo $?
0
$ grep -c 'FROM THE (REPORT|KEYS) PACKET' merged.txt
3
