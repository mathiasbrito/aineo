# Planning probe for wave 1 — 2026-09-23 23:22 CEST
$ nvim --version | head -1
NVIM v0.11.6
$ stylua --version
stylua 2.5.2
$ selene --version
selene 0.31.0
$ claude --version
2.1.281 (Claude Code)
$ git ls-remote --tags https://github.com/nvim-mini/mini.nvim (newest three)
v0.16.0
v0.17.0
v0.18.0
$ grep -n find_files -A1 doc/mini-test.txt (mini.nvim main, fetched raw)
168:      find_files = function()
169-        return vim.fn.globpath('tests', '**/test_*.lua', true, true)
$ grep TESTING.md test targets (mini.nvim main, fetched raw)
121:-- Return test set which will be collected and execute inside `MiniTest.run()`
208:    	nvim --headless --noplugin -u ./scripts/minimal_init.lua -c "lua MiniTest.run()"
