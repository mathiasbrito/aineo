# Implementation

How the specs are being turned into code, wave by wave: the plans an orchestrator dispatches from, kept where every session and every host can read them. `Planning/` says what a feature is and why; the specs say what it must do; this folder says what is being built next, by whom, and whether it landed.

One subfolder today: `Waves/`, with its own `CLAUDE.md`.
