# 2026-09-25 — T7 entry point

**Author:** Mathias Santos de Brito, with Claude — implementer agent (`neovim-lua-developer`); the fix round by a second implementer agent of the same type, which took the branch over at `7774724`; the correction by a third, which took it over at `1740e8e`
**Branch:** `feature/t7-entry` · **Pull request:** #17, into `dev`

## Links

- [[Projects/aineo]] · [[Planning/aineo — v1 agent console]] (C1–C6; D1, D3, D10, D13, D15; R1, R3)
- [[Implementation/Waves/00004-entry/plan]], its brief `brief-t7-entry.md`, its brief review `brief-review.md`, and the evidence `t7-startup-summary.txt`, `t7-summary.txt`
- [[Sessions/2026-09-24 — T3 layout]], [[Sessions/2026-09-24 — T4 Claude session]], [[Sessions/2026-09-24 — T5 report channel]], [[Sessions/2026-09-24 — T6 Send]] — the homes this wires
- [[Sessions/2026-09-23 — T1 tooling foundation]] — the pins this moves, and the stack overflow EP11 fixes

## Context

**Goal:** T7 — "Entry point (C1): prefix mapping, `<Plug>` mappings, `:Aineo`, autostart". The composition root that wires C2–C6 through their entry points into the console the user sees, the autostart under D3 and D15, and the fake's MCP-client mode that proves the wiring end to end (EP9).

## What was done

As the packet left it at `5e2d6c4`; *Fix round* below says what the review of `7774724` changed.

**`plugin/aineo.lua`** — the composition root the modularity table names. At sourcing it defines `:Aineo {send|open|report|input|claude}` (completing its argument), the five `<Plug>(aineo-…)` Normal-mode mappings, and the augroup `aineo` with a `StdinReadPost` and a once-only `VimEnter` autocommand; it requires no aineo module. At `VimEnter` it resolves the configuration (`aineo.config`, the only module it loads then), maps `<prefix>s|o|r|i|c` to the `<Plug>` mappings where the user has not mapped the key sequence, and, when `autostart` is true and the start is bare and interactive, opens the layout two scheduled callbacks later. Open builds the arrangement: the report home given the clock, `stdpath('state')` and `getcwd()` once; the Report buffer; `start_session()` with `mcp_servers(v:servername, v:progpath)`, the allowed report tool and the report instructions; then `layout.open()` in the same tick. Focus builds the same arrangement and calls `layout.focus()`. Send calls `send.send()` from a plain function mapping. Every action runs under one wrapper that tells the user a raised error once, as an error notification without the Lua position. Sourced after startup (a lazy loader), it maps the prefix in the next scheduled callback and starts nothing.

**`lua/aineo/config/init.lua`** (EP11) — the plain copy tracks the tables on its path and raises `opts: contains itself`; a table reached twice without a cycle is still copied, and a refused `setup()` keeps the earlier record.

**`scripts/minimal_init.lua`** — the guard against the real `claude`: `tests/helpers/entry_guard/` first on `PATH`, whose `claude` runs nothing and exits 127; `AINEO_CHILD` removed; and `vim.g.aineo = { autostart = false }` when nothing set it before the init (see *Spec conflicts and widenings*).

**Tests** — `tests/test_entry.lua` (25 cases: `:Aineo`, Open, focus, Send, the `<Plug>` mappings), `tests/test_entry_prefix.lua` (27: the prefix and its timing), `tests/test_entry_startup.lua` (18: the autostart and the dashboards, in UI-attached editors), `tests/test_entry_report.lua` (3: EP9 end to end), `tests/test_entry_guard.lua` (4: the guard), 4 new cases in `tests/test_aineo.lua`, and `tests/test_plugin.lua`'s pins moved (4 cases, one net new). Helpers `tests/helpers/entry.lua` (a 240×42 child, notifications kept, `vim.g.aineo` naming the fake, the windows read back), `tests/helpers/entry_editor.lua` (the brief review's terminal route: an interactive `nvim` in a terminal job of a mini.test child, queried over its `--listen` address; a request to a blocked editor raises with its screen instead of waiting for ever) and its init `tests/helpers/entry_editor_init.lua`. The fake gains the mode `mcp-client`: it reads `--mcp-config`, starts the server entry it names with its command, arguments and environment, replays `tests/fixtures/mcp/claude-code-2.1.281.jsonl` (initialize, initialized, tools/list, the report call), records each answer and exits. Fixtures: `tests/fixtures/entry/dashboard/plugin/entry_dashboard.lua` (the stand-in dashboard, its sources cited in its header) and `tests/fixtures/entry/session.vim`.

## Decisions & reasoning

- **When the prefix is mapped (EP3).** lazy.nvim v11.17.5 (`lua/lazy/core/loader.lua`, `M._load`, read raw at commit `85c7ff3`) adds the plugin to `'runtimepath'`, sources its `plugin/` (`M.packadd`, l.359) and only then runs `config`/`setup(opts)` (`M.config`, l.362); for `lazy = false` all of it runs inside `require('lazy').setup()` in the user's init, before `VimEnter`. So the prefix is mapped at `VimEnter` from the configuration as it stands then; a file sourced after startup maps in the next scheduled callback, after the loader's `setup()` in the same tick. Pinned by `setup()` given through `-c` (which runs before `VimEnter`, measured) and by sourcing late then calling `setup()`.
- **Bare and interactive (EP7, D3).** Measured on Neovim 0.11.6 in the terminal route: at `VimEnter` the server's `v:argv` is `nvim --embed` followed by the user's arguments; `-c` and `+` commands have already run; `StdinReadPost` fired first for piped input. The check: a UI attached, `argc()` 0, no `StdinReadPost`, no `-c`, `-S` or `+…` in `v:argv`, no `$AINEO_CHILD`.
- **Dashboards (EP8, D15).** Read raw at cited versions: snacks.nvim v2.31.0 (`e6fd58c`) opens its dashboard at `UIEnter` (`lua/snacks/init.lua`'s events table; `lua/snacks/dashboard.lua`, `M.setup()`), in buffer 1, under `eventignore=all`; alpha-nvim `4ba26e4` at `VimEnter` (nested), in the current buffer, under `eventignore=all` with its `noautocmd` option; dashboard-nvim `f787e34` at `UIEnter`, in the current buffer when it is empty — at a bare start, the startup buffer — else in a new one, with `bufhidden=wipe` and no `'buftype'` (`buf_local()`), and, without `setup()`, draws nothing until its first run has written a cache file (`db:get_opts()`), so its buffer stays empty; mini.starter at the pin at `VimEnter` under `noautocmd`. The open runs two scheduled callbacks after `VimEnter`, after all of those, and the layout replaces the dashboard's window; nothing detects a dashboard.
- **The guard against the real `claude`** is a `PATH` stub rather than a suite-wide `claude.cmd` naming the fake. The reason first recorded — that the configuration suites keep reading the real defaults — no longer holds as written: two commits later the suites' init also presets `vim.g.aineo = { autostart = false }`, so no suite Neovim leaves `vim.g.aineo` unset (`tests/test_config.lua` passes its inputs explicitly and is unaffected). The guard still wins over naming the fake in that preset because it is `PATH`-wide: it also stops a `claude` any other process a test starts would run, and a test that replaces `vim.g.aineo` whole cannot drop it.
- **The prefix keys are global Normal-mode mappings by default**, against `neovim-lua-developer.md`'s "no global keymaps by default": C1, D1 and D13 are the user's decisions and the brief records that they win (commit `e6462d5`).

## Readings for the MVP review

1. **EP1's message** — `aineo: :Aineo takes one of send, open, report, input, claude`, one `vim.notify` at `ERROR`, for no argument and for any other; `:Aineo send extra` reads as the one argument `send extra` (`nargs = '?'`) and gets the same message. The argument is trimmed, so `:Aineo open ` opens, and `|` separates commands (`bar`), so `:Aineo open | let x = 1` opens, then runs the rest. Completion offers the subcommands that begin with what is typed.
2. **EP4's error messages** — every error an action raises (a wrong setting, `start_session()`'s, Send's own raised errors) reaches the user once as `aineo: <its first line>`, at `ERROR`, without the stack traceback and without Neovim's framing — `Error executing lua: `, the Lua position, `Vim:`: `aineo: layout.report_height: expected a number strictly between 0 and 1, got 2`; `aineo: claude.cmd: 'claude' is not executable` — the Claude home's own line, raised before `jobstart()` (the correction; `jobstart()`'s own `E475: Invalid value for argument cmd: …` was 71 characters for `claude`). Nothing opens. Send's refusals stay its own warnings. A line longer than `v:echospace` still holds a start at a hit-enter prompt: 68 columns on an 80-column screen (measured by the re-measure, Neovim 0.11.6). The not-executable line is 45 characters for `claude` and shows at 80 columns without a prompt (pinned with `claudx`, the same length); by arithmetic from that 68, a command name longer than 29 characters — an absolute path, say — still prompts there.
3. **The report environment is given once**, at the first Open or at the first focus that opens the layout: the Report keeps the working directory of that moment for the editor's life (pinned: a report after `:cd` and a second Open is kept for the first directory), while each new session runs in `getcwd()` as it then is. The state directory is `stdpath('state')` itself; the records land under `<state>/aineo/reports/`.
4. **Only `\o` restarts an exited Claude (R1)** — `\r`, `\i` and `\c` move to their window; when the layout is closed they reopen it around the session's terminal as it is, its exit on screen. They start a session only when the layout must open and there is no terminal to show: before the first start, or once the terminal has been wiped (`:bwipeout!`). With the terminal wiped and the Report open, `\r` moves there and starts nothing.
5. **EP7's unnamed starts** — a start with something to do besides edit refuses the autostart: a short option `-c`, `-S`, `-e` or `-s`, alone or after other short options in one argument, its value attached or not (`-c cmd`, `-ccmd`, `-Rc cmd`, `-S file`, `-e`, `-s keys`), or an argument beginning with `+`; each counts also as another option's value (`--cmd +x`); `--cmd` alone does not refuse. `-E` (improved Ex mode) refuses too (the correction: the re-measure measured it autostarting behind the Ex prompt; pinned as *improved-ex-mode*). `-es` has no UI and never autostarts. `-t tag` and `-q file` do not refuse; the attack review measured `-q`'s file kept as the layout's file column; `-t` inferred. An empty `$AINEO_CHILD` counts as unset: `vim.env` read an empty variable handed through `jobstart()` as nil (measured in this packet's startup probe, Neovim 0.11.6).
6. **A wrong setting at startup** — one error notification, no prefix mapping, no autostart.
7. **EP8's moments** — the open waits two scheduled callbacks after `VimEnter`: it follows what `VimEnter` or `UIEnter` autocommands show directly or from one `vim.schedule()` — pinned for each moment with and without autocommands, with autocommands registered after aineo's, and with the real mini.starter registered before. The layout replaces a startup buffer that is wiped once hidden unless it holds changes, which Neovim refuses to wipe (E37) — then it is kept as the file column; and it never makes Input of an empty buffer wiped once hidden, while an empty startup buffer the user's configuration gave a filetype still becomes Input (the correction, findings 1 and 6 of the re-measure): dashboard-nvim f787e34, which draws into the startup buffer with `bufhidden=wipe` and no `'buftype'`, gives way drawn (after `setup()`) and undrawn (without `setup()`, when its buffer stays empty) — the stand-in models it from its source; the attack review ran the real one. Not covered: a dashboard shown from a timer (`vim.defer_fn`), from two nested schedules, or on a later event; a floating dashboard, which the layout keeps (none of the four opens a float at startup, by their sources).
8. **What EP10 lets load** — sourcing loads no aineo module; `VimEnter` loads `aineo.config` alone (a headless start loads exactly that, pinned under the shipped defaults); a late source loads it in the next scheduled callback.
9. **EP3's "mapped already"** — a global Normal-mode mapping of the key sequence at `VimEnter`; one local to a buffer does not count and keeps winning in its own buffer, aineo's key applying everywhere else; a mapping's keys are compared in both forms Neovim records (`lhsraw` and `lhsrawalt`), so a user's `<C-a>s` stays theirs under `prefix = '<C-a>'` (the correction); a mapping that only overlaps ours (`\` alone, `\sa`) does not count (C7 reports conflicts, T8); a mapping the user makes after `VimEnter` replaces ours, and once ours exists a later `<unique>` mapping of the key fails with E227 (measured by the attack review); `setup()` after `VimEnter` remaps nothing; `prefix = ''` is refused by the configuration, naming `prefix`.
10. **A late source never autostarts** — a lazy loader that sources aineo after `VimEnter` gets the prefix, not the autostart, since `StdinReadPost` passed unseen.
11. **Unknown keys are silent at Open**, in `vim.notify` and in `:messages` alike, left to T8's health check.
12. **The suites' default `vim.g.aineo = { autostart = false }`** — every interactive Neovim a suite starts is a bare start; a test of the autostart sets `vim.g.aineo` before the init, and a test of `setup()` at a startup sets `autostart = true` in `vim.g.aineo` first, since keys `setup()` omits fall back to it.
13. **The guard's `claude` exits 127** with one line on stderr, pinned.
14. **A session restored at startup is kept** — `-S` refuses the autostart, and a session a plugin restores from its own `VimEnter` autocommand (auto-session, persistence.nvim) sets `v:this_session`, which the deferred open checks: the session's windows stay and nothing starts.
15. **`prefix = ''` is refused — the orchestrator's reading of D13, for the user to confirm.** D13 says `prefix` is "a string, or `false`"; refusing `''` reads "prefix" as at least one key, since an empty prefix would remap `s`, `o`, `r`, `i` and `c` themselves. The orchestrator ruled it a reading, not a change of the row (correction brief, 2026-09-25); the re-measure had asked for it to be recorded or converged.

## Spec conflicts and widenings

- **`scripts/minimal_init.lua` gained a third line beyond the brief's two points** (the guard and `AINEO_CHILD`): the default `autostart = false`. Without it, T5's interactive test editors (`tests/helpers/report_tui.lua`) became bare starts that autostarted, took the Report with `stdpath('state')`, and failed `tests/test_mcp_blocked_editor.lua` (measured by the records review on the final code: without the line, `tests/test_mcp_blocked_editor.lua` fails 1 of its 3 cases and writes a file `v:null` into the checkout's root; the "2 fails" first recorded here came from a mid-loop run, `t7-full-3`, whose second fail was a T1 pin not yet moved, not the missing line). T5's helper is outside the packet; the alternative, `autostart = false` in that helper, was the orchestrator's to choose, and **the orchestrator decided the widening stands** (fix-round brief, 2026-09-25).
- **Three isolation documents** — the root `CLAUDE.md`, `.claude/agents/neovim-lua-developer.md` and `.claude/agents/neovim-claude-code-integrator.md` (whose "no test depends on `PATH` order" the guard now contradicts) — do not mention the `PATH` guard, the `AINEO_CHILD` removal or the autostart preset. None is this packet's; the orchestrator holds the `ai/` follow-up (records review R1).
- **`tests/helpers/entry_guard/claude`** is a file inside a new directory whose name begins with `entry`.

## Limits

- lazy.nvim, snacks.nvim, alpha-nvim and dashboard-nvim were read, not run, by this packet; the stand-ins model them. The attack review ran all four for real (snacks v2.31.0, alpha `4ba26e4`, dashboard-nvim `f787e34`, mini.starter at the pin) and found dashboard-nvim did not give way: the first stand-in gave it `buftype=nofile` and a new buffer, neither of which it has at a bare start. The fix round's stand-in takes its options from its source. mini.starter is the real one.
- At 80 columns a message longer than `v:echospace` (68) holds a bare start at a hit-enter prompt. The not-executable line fits there for a command name of up to 29 characters (reading 2); a longer `claude.cmd` — an absolute path — still prompts. Pinned at 80 columns with a 6-character name only.
- `tests/helpers/entry_editor.lua` raises when it finds the editor blocked, but a request sent the moment before the editor blocks waits for ever: a startup case whose editor blocks can stall until the run limit rather than fail at once (seen twice in the fix round; bounded with `AINEO_TEST_RUN_LIMIT_MS`).
- A full run of the suite leaves nothing behind while the autostart preset is in; without it `tests/test_mcp_blocked_editor.lua` (T5) writes a file `v:null` into the checkout's root (see *Open threads*).
- The real `claude` never ran.

## Red, green and mutants

The unit list, in order: G the guard (the `claude` first on `PATH`, `AINEO_CHILD` removed); EP11; EP1 the command (usage, completion); EP4 Open (the layout, the settings handed to the session, a wrong setting, unknown keys, again, after an exit); EP5 focus; EP6 Send; EP2 the `<Plug>` mappings; EP3 the prefix (default, `vim.g.aineo`, `setup()`, `false`, the user's mapping, a wrong setting, a late source); EP10 the moved pins; EP7 the autostart and each refusal; EP8 the dashboards; EP9 end to end; the suites' autostart default.

**Seen red — 39 cases**, each for the missing behaviour:
- the guard (written together, red together — a deviation from one-at-a-time, recorded): *run the guard as claude* (`Left: ~/.local/bin/claude`), *… in a child whose PATH starts with another claude* (`Left:` the fixture's `claude`), *remove AINEO_CHILD from a child* (`Left: "1" Right: vim.NIL`);
- *refuses options that contain themselves, naming them* — `stack overflow`;
- *`:Aineo` without an argument …* — `E492: Not an editor command: Aineo`; *completes its argument to its five subcommands* — `Left: {}`;
- *`:Aineo open` starts Claude in the layout …* — `Left: { "" }`; *with a wrong setting tells the user once …* — the raw Lua callback error;
- *`:Aineo report, input and claude` move the cursor …* ×3 — `Left: ""` (the first arrange left the cursor in Input, so the input case was not red; the arrange was changed to a new tab and all three were seen red);
- *`:Aineo send` sends Input's text* — `Left: ""`; *`<Plug>(aineo-open)` starts Claude …* — `Left: { "" }`;
- *the prefix is `\` by default* ×5 — `Left: ""`; *is the one setup() records right after the file is sourced late* ×5 — `Left: false`;
- *a bare interactive start opens the layout around a new Claude* — `Left: { "" }`; the refusals *headless*, *file*, *piped-stdin*, *in-aineos-claude*, *command*, *plus-command*, *session* — each `Left: 1 Right: 0`, each red before its condition was written;
- *sourced after a bare interactive start maps the prefix and starts no Claude* — `Left: 1`;
- *a startup dashboard gives way …* ×6 — `Left: 1` (the dashboard's filetype on screen) or the Input window showing the dashboard's buffer;
- *start aineo in no interactive editor whose test left vim.g.aineo unset* — `Left: { "terminal", "aineo://report", "aineo://input" }`.

**Arrived green — 45 cases** (42 new, and T1's three pins as moved: one rewritten, one replaced, one new), each pinning code written ahead of it or behaviour of the homes, with its killer (table below): the three companion EP11 cases (CFG-key, CFG-shared, CFG-keep); *completes the subcommands that begin with what is typed* (E-complete); *with another argument …* (E-usage-level); Open's *report server* (M21-open), *tools* (E-tools), *instructions* (E-instructions), *working directory* (E-cwd), *unknown key* (E-unknown-warns), *again while Claude runs* (E-no-restore), *after Claude has exited* (E-cached-arrangement); focus *opens the layout … when it is closed* ×3 (E-focus-needs-layout); the `<Plug>` focus cases ×3 (E-plug-focus); `<Plug>(aineo-send)` (E-plug-expr, E-send-unwired); the prefix from `vim.g.aineo` ×5 and from `setup()` ×5 (P1, P2), `false` ×5 (P3), the user's mapping (M20), a wrong setting at startup (P5); T1's moved pins (TP1, TP2, TP3); *autostart-off* (ST-autostart); mini.starter ×2 (M23, ST-autostart); EP9 *shows its report* (M21, FAKE-call), *keeps … under the state directory* (E-state-dir, E-report-cwd), *stamps … local time* (E-clock).

**Mutant table** — literal edits in `t7-mutants.py` (the orchestrating session's scratchpad), one at a time, each file restored byte for byte from the pristine copy, each run as `make test_file` on a copy of its test file narrowed to the named groups under `.tests/t7-narrow/`; the kind read from mini.test's output ("Failed expectation" is an assertion). Run on the code head `5e2d6c4`, after the last edit to any file a mutant or its tests touch.

| # | File | Edit (old → new, `⏎` a line break) | Narrowed to | Result |
|---|---|---|---|---|
| M19 | `plugin/aineo.lua` | `return #vim.api.nvim_list_uis() > 0⏎` → `return true⏎` | `tests/test_entry_startup.lua` › a start that is not bare and interactive (8 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| ST-file | `plugin/aineo.lua` | `and vim.fn.argc() == 0⏎` → *(removed)* | `tests/test_entry_startup.lua` › a start that is not bare and interactive (8 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| ST-stdin | `plugin/aineo.lua` | `and not read_stdin⏎` → *(removed)* | `tests/test_entry_startup.lua` › a start that is not bare and interactive (8 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| M22 | `plugin/aineo.lua` | `and vim.env.AINEO_CHILD == nil⏎` → *(removed)* | `tests/test_entry_startup.lua` › a start that is not bare and interactive (8 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| ST-autostart | `plugin/aineo.lua` | `if config.autostart and is_bare_interactive_start() then` → `if is_bare_interactive_start() then` | `tests/test_entry_startup.lua` › a start that is not bare and interactive, mini.starter (10 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| ST-c | `plugin/aineo.lua` | `argument == '-c' or` → *(removed)* | `tests/test_entry_startup.lua` › a start that is not bare and interactive (8 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| ST-S | `plugin/aineo.lua` | `argument == '-S' or` → *(removed)* | `tests/test_entry_startup.lua` › a start that is not bare and interactive (8 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| ST-plus | `plugin/aineo.lua` | `or vim.startswith(argument, '+')` → *(removed)* | `tests/test_entry_startup.lua` › a start that is not bare and interactive (8 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| M23 | `plugin/aineo.lua` | `vim.schedule(function()⏎ run(open)⏎ end)` → `vim.schedule(function()⏎ if not vim.tbl_contains({ 'snacks_dashboard', 'alpha', 'dashboard', 'ministarter' }, vim.bo.filetype) then⏎ run(open)⏎ end⏎ end)` | `tests/test_entry_startup.lua` › a startup dashboard, mini.starter (8 cases) | killed: 7 failing, 7 by assertion, 0 otherwise |
| D-one-schedule | `plugin/aineo.lua` | `vim.schedule(function()⏎ vim.schedule(function()⏎ run(open)⏎ end)⏎ end)` → `vim.schedule(function()⏎ run(open)⏎ end)` | `tests/test_entry_startup.lua` › a startup dashboard, mini.starter (8 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| D-no-schedule | `plugin/aineo.lua` | `␣␣␣␣open_after_dashboards()⏎` → `␣␣␣␣run(open)⏎` (the call in `start_up()`, four spaces of indentation; the name also opens `local function open_after_dashboards()`) | `tests/test_entry_startup.lua` › a startup dashboard, mini.starter (8 cases) | killed: 6 failing, 6 by assertion, 0 otherwise |
| L-autostarts | `plugin/aineo.lua` | `run(function()⏎ map_prefix(resolved_config().prefix)⏎ end)` → `run(start_up)` | `tests/test_entry_startup.lua` › sourced after a bare interactive start (1 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| L-unscheduled | `plugin/aineo.lua` | `vim.schedule(function()⏎ run(function()⏎ map_prefix(resolved_config().prefix)⏎ end)⏎ end)` → `run(function()⏎ map_prefix(resolved_config().prefix)⏎ end)` | `tests/test_entry_prefix.lua` › the prefix (25 cases) | killed: 5 failing, 5 by assertion, 0 otherwise |
| P1 | `plugin/aineo.lua` | `map_prefix(config.prefix)⏎` → `map_prefix('\\')⏎` | `tests/test_entry_prefix.lua` › the prefix (25 cases) | killed: 15 failing, 15 by assertion, 0 otherwise |
| P2 | `plugin/aineo.lua` | `vim.api.nvim_create_autocmd('VimEnter', {` → `run(start_up)⏎ vim.api.nvim_create_autocmd('User', {` | `tests/test_entry_prefix.lua` › the prefix (25 cases) | killed: 5 failing, 5 by assertion, 0 otherwise |
| P3 | `plugin/aineo.lua` | `if prefix == false then⏎ return⏎ end⏎` → *(removed)* | `tests/test_entry_prefix.lua` › the prefix (25 cases) | killed: 5 failing, 5 by assertion, 0 otherwise |
| M20 | `plugin/aineo.lua` | `if vim.fn.maparg(keys, 'n') == '' then` → `if true then` | `tests/test_entry_prefix.lua` › the user's own mapping (1 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| P5 | `plugin/aineo.lua` | `callback = function()⏎ run(start_up)⏎ end,` → `callback = function()⏎ start_up()⏎ end,` | `tests/test_entry_prefix.lua` › a wrong setting at startup (1 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-position | `plugin/aineo.lua` | `return (message:gsub('^.-%.lua:%d+: ', ''))` → `return message` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-pcall | `plugin/aineo.lua` | `local succeeded, failure = pcall(action)` → `local succeeded, failure = true, action()` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-usage-level | `plugin/aineo.lua` | `vim.notify(USAGE, vim.log.levels.ERROR)` → `vim.notify(USAGE, vim.log.levels.WARN)` | `tests/test_entry.lua` › :Aineo (4 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| E-complete | `plugin/aineo.lua` | `return vim.startswith(subcommand, argument_lead)` → `return true` | `tests/test_entry.lua` › :Aineo (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-unknown-warns | `plugin/aineo.lua` | `return (config.resolve_config(vim.g.aineo, config.recorded_setup_options()))` → `local resolved, unknown = config.resolve_config(vim.g.aineo, config.recorded_setup_options())⏎ if #unknown > 0 then⏎ vim.notify('aineo: unknown ' .. table.concat(unknown, ', '), vim.log.levels.WARN)⏎ end⏎ return resolved` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| M21 | `plugin/aineo.lua` | `mcp.mcp_servers(vim.v.servername, vim.v.progpath)` → `mcp.mcp_servers('', vim.v.progpath)` | `tests/test_entry_report.lua` › the report tool (3 cases) | killed: 3 failing, 3 by assertion, 0 otherwise |
| M21-open | `plugin/aineo.lua` | `mcp.mcp_servers(vim.v.servername, vim.v.progpath)` → `mcp.mcp_servers('', vim.v.progpath)` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-tools | `plugin/aineo.lua` | `allowed_tools = mcp.allowed_mcp_tools(),` → `allowed_tools = {},` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-instructions | `plugin/aineo.lua` | `instructions = report.report_instructions(mcp.report_tool_name()),` → `instructions = '',` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-cwd | `plugin/aineo.lua` | `cwd = vim.fn.getcwd(),` → `cwd = vim.uv.os_tmpdir(),` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-cached-arrangement | `plugin/aineo.lua` | `local function open()⏎ require('aineo.layout').open(arrangement(resolved_config()))⏎end` → `local cached_arrangement⏎local function open()⏎ cached_arrangement = cached_arrangement or arrangement(resolved_config())⏎ require('aineo.layout').open(cached_arrangement)⏎end` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-focus-role | `plugin/aineo.lua` | `focus('report')` → `focus('input')` | `tests/test_entry.lua` › :Aineo report, input and claude (6 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| E-focus-needs-layout | `plugin/aineo.lua` | `local function focus(role)⏎` → `local function focus(role)⏎ if require('aineo.layout').input_buffer() == nil then⏎ return⏎ end⏎` | `tests/test_entry.lua` › :Aineo report, input and claude (6 cases) | killed: 3 failing, 3 by assertion, 0 otherwise |
| E-send-unwired | `plugin/aineo.lua` | `require('aineo.send').send()⏎` → *(removed)* | `tests/test_entry.lua` › :Aineo send, <Plug>(aineo-send) (2 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| E-plug-expr | `plugin/aineo.lua` | `end, { desc = 'aineo: ' .. subcommand })⏎end⏎⏎--- The key` → `end, { desc = 'aineo: ' .. subcommand, expr = true })⏎end⏎⏎--- The key` | `tests/test_entry.lua` › <Plug>(aineo-send) (1 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-plug-missing | `plugin/aineo.lua` | `for _, subcommand in ipairs(SUBCOMMANDS) do⏎ vim.keymap.set('n', plug_mapping(subcommand)` → `for _, subcommand in ipairs({ 'send', 'report', 'input', 'claude' }) do⏎ vim.keymap.set('n', plug_mapping(subcommand)` | `tests/test_entry.lua` › <Plug>(aineo-open) (1 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-state-dir | `plugin/aineo.lua` | `state_directory = vim.fn.stdpath('state'),` → `state_directory = vim.fn.stdpath('cache'),` | `tests/test_entry_report.lua` › the report tool (3 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| E-report-cwd | `plugin/aineo.lua` | `working_directory = vim.fn.getcwd(),` → `working_directory = '/',` | `tests/test_entry_report.lua` › the report tool (3 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-clock | `plugin/aineo.lua` | `return os.date('%Y-%m-%dT%H:%M:%S')` → `return '2000-01-01T00:00:00'` | `tests/test_entry_report.lua` › the report tool (3 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| TP1 | `plugin/aineo.lua` | `vim.g.loaded_aineo = true⏎` → `vim.g.loaded_aineo = true⏎require('aineo.config')⏎` | `tests/test_plugin.lua` › plugin/aineo.lua (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| TP2 | `plugin/aineo.lua` | `local config = resolved_config()⏎ map_prefix(config.prefix)⏎` → `local config = resolved_config()⏎ require('aineo.layout')⏎ map_prefix(config.prefix)⏎` | `tests/test_plugin.lua` › plugin/aineo.lua (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| CFG-key | `lua/aineo/config/init.lua` | `copy[plain_copy(key, enclosing)] = plain_copy(inner_value, enclosing)` → `copy[plain_copy(key)] = plain_copy(inner_value, enclosing)` | `tests/test_aineo.lua` › setup() (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| CFG-shared | `lua/aineo/config/init.lua` | `enclosing[value] = nil⏎` → *(removed)* | `tests/test_aineo.lua` › setup() (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| CFG-keep | `lua/aineo/config/init.lua` | `recorded_setup_options = plain_copy(setup_options)` → `recorded_setup_options = {}⏎ recorded_setup_options = plain_copy(setup_options)` | `tests/test_aineo.lua` › setup() (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| CFG-name | `lua/aineo/config/init.lua` | `error('opts: contains itself', 0)` → `error('contains itself', 0)` | `tests/test_aineo.lua` › setup() (13 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| G1 | `scripts/minimal_init.lua` | `vim.env.PATH = vim.fs.joinpath(checkout, 'tests', 'helpers', 'entry_guard') .. ':' .. vim.env.PATH⏎` → *(removed)* | `tests/test_entry_guard.lua` › the suites (4 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| G2 | `scripts/minimal_init.lua` | `vim.env.PATH = vim.fs.joinpath(checkout, 'tests', 'helpers', 'entry_guard') .. ':' .. vim.env.PATH` → `vim.env.PATH = vim.env.PATH .. ':' .. vim.fs.joinpath(checkout, 'tests', 'helpers', 'entry_guard')` | `tests/test_entry_guard.lua` › the suites (4 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| G3 | `scripts/minimal_init.lua` | `⏎vim.env.AINEO_CHILD = nil⏎` → `⏎` | `tests/test_entry_guard.lua` › the suites (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-no-restore | `plugin/aineo.lua` | `local function open()⏎` → `local function open()⏎ if require('aineo.claude').session_status() == 'starting' or require('aineo.claude').session_status() == 'ready' then⏎ return⏎ end⏎` | `tests/test_entry.lua` › :Aineo open (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| E-plug-focus | `plugin/aineo.lua` | `run(ACTIONS[subcommand])` → `run(ACTIONS.send)` | `tests/test_entry.lua` › <Plug>(aineo-report), (aineo-input) and (aineo-claude) (3 cases) | killed: 3 failing, 3 by assertion, 0 otherwise |
| TP3 | `plugin/aineo.lua` | `for _, subcommand in ipairs(SUBCOMMANDS) do⏎ vim.keymap.set('n', plug_mapping(subcommand)` → `for _, subcommand in ipairs({ 'send', 'report', 'input', 'claude' }) do⏎ vim.keymap.set('n', plug_mapping(subcommand)` | `tests/test_plugin.lua` › plugin/aineo.lua (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| FAKE-call | `tests/helpers/fake_claude.lua` | `['mcp-client'] = { screens = { STARTUP }, calls_report_tool = true },` → `['mcp-client'] = { screens = { STARTUP } },` | `tests/test_entry_report.lua` › the report tool (3 cases) | killed: 3 failing, 3 by assertion, 0 otherwise |

**Tally:** 50 mutant runs of 48 distinct literal edits — `M21` and `M21-open` are one edit run against two test files, and so are `E-plug-missing` and `TP3` — 50 killed by an assertion, no crash, no survivor. The literals are printed with runs of white space collapsed to one space, but where the table marks indentation (`␣`); the driver `t7-mutants.py` holds each literal exactly, and refuses one that does not match exactly once. The brief's M19–M23 are rows M19, M20, M21 (and M21-open), M22 and M23; M19 was run narrowed, with the `PATH` guard in place. In an earlier pass on `6476e44`, `E-pcall` and `CFG-shared` were killed by a crash and `E-clock` survived; the fourth commit made the first two assertions and pinned the clock, and the three were killed by assertion on the final head (rows above).

**Suites on the head `5e2d6c4`:** `make test` 573 cases, `Fails (0)`, exit 0, 03:33:43–03:40:48 CEST (425 s) — the baseline's 491 plus 82; `make lint` clean (StyLua check, selene 0 errors, 0 warnings); the modularity deep-require check prints only intra-home lines (`lua/aineo/{claude,layout,mcp,report}/…`), none from `plugin/`, `tests/` or `scripts/`.

## Fix round — the review of `7774724`

The three-fold review of pull request #17 at `7774724` (attack A1–A10, test integrity I1–I8, records R1–R12) came back to a second implementer agent, which took the branch over; the packet's author was not continued (its context had reached about 462 K). The round's code is commits `b5e594c` and `b50c145`; its records, this section and the corrections above, a third commit.

**Boundary, widened by the orchestrator for this round only:** `lua/aineo/layout/` and `tests/test_layout*.lua` for A2 and A6 (D15 and R1 unchanged; every T3 case kept, new cases only), and `lua/aineo/config/` with `tests/test_config.lua` for A8.

**What changed, by finding**

- **A1** — `run()` tells the user the error's first line, without Neovim's framing (`Error executing lua: `, the Lua position, `Vim:`): `aineo: E475: Invalid value for argument cmd: 'claude' is not executable`. The attack reviewer's measured one-line fix is adopted; stripping the framing is this round's, because the fix alone still held an 80-column bare start at a hit-enter prompt (measured with a 97-character line). The stripped line still prompts at 80 columns when longer than `v:echospace` — 68, measured — which the default `claude`'s 71 characters are (reading 2; *Limits*).
- **A2** — `layout.build()` keeps the startup window's buffer as a file only when it is not wiped once hidden (`is_file_to_keep()`), and `is_unnamed_and_empty()` requires no filetype: the attack reviewer's two measured checks. dashboard-nvim `f787e34`'s stand-in now takes `bufhidden=wipe`, `nobuflisted`, `noswapfile`, the filetype and no `'buftype'` from `buf_local()`, the startup buffer (which is empty at a bare start), and a drawn theme left unmodified and not modifiable — or no drawing, as without `setup()`; this also answers I8, whose header said "a new buffer unless the current one is empty" while its rows used a new buffer.
- **A3, A7** — the refusal is a pattern of short options, `^%-%a*[cSes]`: `-c`, `-S`, `-e`, `-s`, alone or bundled (`-Rc`), their value attached or not (`-clet …`).
- **A4** — the deferred open runs only while `v:this_session` is empty, so a session a plugin restores at `VimEnter` keeps its windows; pinned by a stand-in session plugin registered after aineo, so the check must be the deferred one.
- **A5** — a key sequence counts as mapped only when a global Normal-mode mapping has it (`nvim_get_keymap('n')`, compared by `lhsraw`); R3's scenario, a buffer-local `<LocalLeader>s` from a `FileType` autocommand, pins it.
- **A6** — only `\o` restarts. The composition root keeps the terminal of the session it started last; a focus reopens the layout around that terminal, exited or not, and starts a session only when the layout must open and that terminal is gone. **Chosen: `aineo.layout.focus()` takes the arrangement or a function returning it, called only when the layout opens** — not a query export: the decision whether to open already lives in the layout, and a query would have made the composition root repeat it. T3's callers, which pass a table, are unchanged.
- **A8** — `prefix = ''` is refused by `resolve_config()`: `prefix: expected a non-empty string, or false`.
- **A9** — `:Aineo` takes `bar`; with it Neovim drops the trailing space of `:Aineo open `, so no trim is needed (a trim first added was measured dead by its surviving mutant and removed in `b50c145`).
- **A10** — a stand-in plugin manager calls `setup({ autostart = false })` after aineo's `plugin/` file, over `vim.g.aineo = { autostart = true }`, which overrides the suites' preset.
- **A-give-once** — the reviewer's surviving mutant, run as its literal edit against this round's report group and the whole suite (table below), survives; equivalent by the report home's contract, with an input built for it: the report home reads the working and state directories only when it first creates the Report (`report_buffer()`, `lua/aineo/report/init.lua`), which the composition root calls right after giving them, and calls the clock anew for each report, so giving them again changes nothing observable. The input: an Open, a `:cd`, a second Open after the first Claude exited, and a report from the second — kept for the first directory with and without the mutant (*keeps Claude's report for the working directory of the first Open, after a :cd*). The flag stays. **Restated by the correction:** the mutant is equivalent on every path short of reloading the module — the re-measure built a separating input (an Open, `package.loaded['aineo.report'] = nil`, `:tabnew`, `:tabonly`, a second Open: the head reports `aineo.report has no environment` and opens nothing, the mutant opens the layout), which no user path takes; the "only if the first `report_buffer()` raised" argument above was wrong. The correction kept the flag rather than drop it (see *Correction*).
- **I1–I7** — the integrity reviewer's probes, adopted as cases: the typed `\o` and `\r` (I1); the EP10 pin started with `--cmd 'lua vim.g.aineo = {}'` (I2); a clock stub that answers only the local format (I3); the definitions pin over every mode as `<mode> <lhs>` (I4); the guard run from a child, its status 127 and its stderr line (I5); `:messages` checked with `vim.notify` (I6); `:Aineo open extra` (I7).
- **Plan IDs leave the source** (R9, the orchestrator's decision): `plugin/aineo.lua`'s docstrings cited D3, C1 and D15.

**The unit list**, in order: A1 the error line (headless, then at a bare start); A8 the empty prefix; A9 the trailing space, then the bar; I7, I6 and I1 (the reviewer's probes); A6 a focus after an exit with the layout open, then closed; A5 a buffer-local mapping; I4, I2, I3 and I5; A3 `-clet…`, then `-Rc`; A7 `-e`, then `-s`; A4 a plugin-restored session; A2 in the layout (a buffer wiped once hidden, then an empty buffer with a filetype), then the dashboard-nvim stand-in; A10 `setup()` at a startup; A-give-once's input; then, found by the round's own wiped-terminal case, the layout's lazy arrangement and a focus with the terminal wiped.

**Seen red — 23 cases**, each for the missing behaviour:
- *`:Aineo open` › with a command that cannot run tells the user once, on one line naming it* — `Left:` the eight-line traceback message; retargeted to the framing-free line, red again with `Left: "aineo: Error executing lua: Vim:E475: …"`;
- *`resolve_config()` › refuses an empty prefix, which would map the keys it prefixes themselves* — `Observed no error`;
- *`:Aineo` › with a subcommand and a space after it runs the subcommand*, *› followed by a bar runs the subcommand, then the command after the bar* — each `Left: "" Right: "terminal"`;
- *`:Aineo report, input and claude` › after Claude has exited move to their window and keep its exit on screen* ×3 and *› after Claude has exited reopen the closed layout around its exit* ×3 — each `Left: 5 Right: 3` (the restarted terminal); the two were written together and each run red on its own — a deviation from one failing test at a time;
- *the user's own mapping › local to the buffer the editor starts in stays there, and the key is mapped in every other buffer* — `Left: "" Right: "<Plug>(aineo-send)"`;
- *a start that is not bare and interactive › starts no Claude* for *command-in-one-argument*, *command-among-short-options*, *ex-mode*, *keys-from-a-script* and *session-restored-by-a-plugin* — each `Left: 1 Right: 0`, each before its part of the pattern or check existed;
- *`open()` › replaces a buffer wiped once hidden, as a startup dashboard's, rather than keep it as a file* — `Left: 4 Right: 3` (its first run raised E37 because the test left the buffer modified; dashboard-nvim's themes leave it unmodified, and the test was corrected to model that before the red was taken); *`open()` › makes a new Input when the empty buffer Neovim starts with is a startup dashboard's* — `Left: false Right: true`; the two were written together and each run red on its own;
- *a startup dashboard › gives way …* for *dashboard-nvim-at-UIEnter* and *dashboard-nvim-scheduled* (rows rewritten to the new model) — `Left: { "terminal", "", "aineo://report", "aineo://input" }`, and *dashboard-nvim-without-setup-at-UIEnter* — `Left: 1 Right: 0`; run against the layout at `HEAD` with the fix held aside;
- *focus() with its window closed opens the layout with the arrangement a function returns* — `arrangement: expected table, got function`;
- *`:Aineo report` after Claude has exited and its terminal was wiped › moves to the open Report and starts nothing* — `Left: "starting" Right: "exited"`.

**Arrived green — 13 cases**, each with its killer in the table below: *a bare interactive start › with a command that cannot run tells the user on one line, without a prompt* (pinning A1's code: R-A1-framing-start); *› starts no Claude when setup() turns autostart off over vim.g.aineo* (R-A10-setup); *`:Aineo` › with a subcommand and more …* (USAGE-extra); *`:Aineo open` › with a key no setting knows opens without a word*, now with `:messages` (UNKNOWN-echo); *the prefix typed* ×2 (PX-expr, PX-script); the EP10 pin (LOAD-AUTOSTART); the definitions pin (X-MODE); the local-time case (CLOCK-UTC); *the suites › run the guard as claude, which refuses on stderr with 127, in a child* (GUARD-STATUS); *keeps Claude's report for the working directory of the first Open, after a :cd* (M21; A-give-once survives it, above); *focus() with its window open never calls the function it is given for the arrangement* (R-A6-layout-eager); *after … its terminal was wiped › starts a new Claude when the layout is closed* (R-A6-valid). Four of these are existing cases strengthened (I2, I3, I4, I6); the rest are new. *a wrong setting at startup* keeps its case with the refusal's new wording, and the four snacks and alpha dashboard rows gain the stand-in's new fields (killed by M23).

**Tried and not kept:** a startup pin at 80 columns. At 80 columns the framing-free line still prompts (reading 2), so the pin could not pass; its red run stalled until the runner's time limit (`AINEO_TEST_RUN_LIMIT_MS=60000`) instead of failing, because the harness's blocked-editor check can race the editor's prompt (*Limits*). The pin runs at the suites' 240 columns.

**Mutants** — each a literal edit applied to a pristine copy of its file (the round committed first), run as `make test_file` on a copy of its test file narrowed to the named groups under `.tests/t7f-mut-narrow/`, one at a time, the file restored and compared byte for byte; the kind read from mini.test's output. Driver `t7f-mutants.py` and list `t7f_mutant_list.py` (the orchestrating session's scratchpad). Run on the final code head `b50c145`:

| # | File | Edit (old → new) | Run against | Result |
|---|---|---|---|---|
| M19 | `plugin/aineo.lua` | `return #vim.api.nvim_list_uis() > 0⏎` → `return true⏎` | `tests/test_entry_startup.lua` › a start that is not bare and interactive (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| M20 | `plugin/aineo.lua` | `if not has_global_mapping(keys) then` → `if true then` | `tests/test_entry_prefix.lua` › the user's own mapping (2 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| M21 | `plugin/aineo.lua` | `mcp.mcp_servers(vim.v.servername, vim.v.progpath)` → `mcp.mcp_servers('', vim.v.progpath)` | `tests/test_entry_report.lua` › the report tool (4 cases) | killed: 4 failing, 4 by assertion, 0 otherwise |
| M22 | `plugin/aineo.lua` | `    and vim.env.AINEO_CHILD == nil⏎` → *(removed)* | `tests/test_entry_startup.lua` › a start that is not bare and interactive (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| M23 | `plugin/aineo.lua` | `    vim.schedule(open_unless_session_restored)⏎` → `    vim.schedule(function()⏎      if not vim.tbl_contains({ 'snacks_dashboard', 'alpha', 'dashboard', 'ministarter' }, vim.bo.filetype) then⏎        open_unless_session_restored()⏎      end⏎    end)⏎` | `tests/test_entry_startup.lua` › a startup dashboard, mini.starter (9 cases) | killed: 8 failing, 8 by assertion, 0 otherwise |
| PX-expr | `plugin/aineo.lua` | `plug_mapping(subcommand), { desc = 'aineo: ' .. subcommand })` → `plug_mapping(subcommand), { desc = 'aineo: ' .. subcommand, expr = true })` | `tests/test_entry.lua` › the prefix typed (2 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| PX-script | `plugin/aineo.lua` | `plug_mapping(subcommand), { desc = 'aineo: ' .. subcommand })` → `plug_mapping(subcommand), { desc = 'aineo: ' .. subcommand, script = true })` | `tests/test_entry.lua` › the prefix typed (2 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| LOAD-AUTOSTART | `plugin/aineo.lua` | `if config.autostart and is_bare_interactive_start() then` → `if config.autostart and require('aineo.layout') and is_bare_interactive_start() then` | `tests/test_plugin.lua` › plugin/aineo.lua (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| CLOCK-UTC | `plugin/aineo.lua` | `return os.date('%Y-%m-%dT%H:%M:%S')` → `return os.date('!%Y-%m-%dT%H:%M:%S')` | `tests/test_entry_report.lua` › the report tool (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| X-MODE | `plugin/aineo.lua` | `vim.keymap.set('n', keys, plug_mapping(subcommand)` → `vim.keymap.set({ 'n', 'x' }, keys, plug_mapping(subcommand)` | `tests/test_plugin.lua` › plugin/aineo.lua (4 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| GUARD-STATUS | `tests/helpers/entry_guard/claude` | `exit 127⏎` → `exit 0⏎` | `tests/test_entry_guard.lua` › the suites (5 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| UNKNOWN-echo | `plugin/aineo.lua` | `  return (config.resolve_config(vim.g.aineo, config.recorded_setup_options()))⏎` → `  local resolved, unknown = config.resolve_config(vim.g.aineo, config.recorded_setup_options())⏎  if #unknown > 0 then⏎    vim.api.nvim_echo({ { 'aineo: unknown ' .. table.concat(unknown, ', '), 'WarningMsg' } }, true, {})⏎  end⏎  return resolved⏎` | `tests/test_entry.lua` › :Aineo open (10 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| USAGE-extra | `plugin/aineo.lua` | `  local action = ACTIONS[command.args]` → `  local action = ACTIONS[vim.split(command.args, ' ')[1]]` | `tests/test_entry.lua` › :Aineo (7 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| A-give-once | `plugin/aineo.lua` | `  if report_environment_given then` → `  if false then` | `tests/test_entry_report.lua` › the report tool (4 cases), then the whole suite (603) | survived both (whole suite: 603 cases, `Fails (0)`); equivalent, above |
| R-A1-framing | `plugin/aineo.lua` | `local ERROR_FRAMING = { '^Error executing lua: ', '^.-%.lua:%d+: ', '^Vim:' }` → `local ERROR_FRAMING = { '^.-%.lua:%d+: ' }` | `tests/test_entry.lua` › :Aineo open (10 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A1-framing-start | `plugin/aineo.lua` | `local ERROR_FRAMING = { '^Error executing lua: ', '^.-%.lua:%d+: ', '^Vim:' }` → `local ERROR_FRAMING = { '^.-%.lua:%d+: ' }` | `tests/test_entry_startup.lua` › a bare interactive start (3 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A1-first-line | `plugin/aineo.lua` | `  local line = message:match('^[^\n]*')` → `  local line = message` | `tests/test_entry.lua` › :Aineo open (10 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A8-empty | `lua/aineo/config/init.lua` | `return (type(value) == 'string' and value ~= '') or value == false` → `return type(value) == 'string' or value == false` | `tests/test_config.lua` › resolve_config() (31 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A9-bar | `plugin/aineo.lua` | `  bar = true,⏎` → *(removed)* | `tests/test_entry.lua` › :Aineo (7 cases) | killed: 2 failing, 2 by assertion, 0 otherwise |
| R-A5-global | `plugin/aineo.lua` | `if not has_global_mapping(keys) then` → `if vim.fn.maparg(keys, 'n') == '' then` | `tests/test_entry_prefix.lua` › the user's own mapping (2 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A3-bundle | `plugin/aineo.lua` | `local TASK_OPTION = '^%-%a*[cSes]'` → `local TASK_OPTION = '^%-[cSes]'` | `tests/test_entry_startup.lua` › a start that is not bare and interactive (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A3-attached | `plugin/aineo.lua` | `local TASK_OPTION = '^%-%a*[cSes]'` → `local TASK_OPTION = '^%-%a*[cSes]$'` | `tests/test_entry_startup.lua` › a start that is not bare and interactive (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A7-e | `plugin/aineo.lua` | `local TASK_OPTION = '^%-%a*[cSes]'` → `local TASK_OPTION = '^%-%a*[cSs]'` | `tests/test_entry_startup.lua` › a start that is not bare and interactive (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A7-s | `plugin/aineo.lua` | `local TASK_OPTION = '^%-%a*[cSes]'` → `local TASK_OPTION = '^%-%a*[cSe]'` | `tests/test_entry_startup.lua` › a start that is not bare and interactive (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A4-session | `plugin/aineo.lua` | `  if vim.v.this_session == '' then` → `  if true then` | `tests/test_entry_startup.lua` › a start that is not bare and interactive (13 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A2-wiped | `lua/aineo/layout/init.lua` | `  return is_file(buffer) and vim.bo[buffer].bufhidden ~= 'wipe'` → `  return is_file(buffer)` | `tests/test_layout.lua` › open() (29 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A2-wiped-start | `lua/aineo/layout/init.lua` | `  return is_file(buffer) and vim.bo[buffer].bufhidden ~= 'wipe'` → `  return is_file(buffer)` | `tests/test_entry_startup.lua` › a startup dashboard, mini.starter (9 cases) | killed: 3 failing, 3 by assertion, 0 otherwise |
| R-A2-filetype | `lua/aineo/layout/init.lua` | `    and vim.bo[buffer].filetype == ''⏎` → *(removed)* | `tests/test_layout.lua` › open() (29 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A2-filetype-start | `lua/aineo/layout/init.lua` | `    and vim.bo[buffer].filetype == ''⏎` → *(removed)* | `tests/test_entry_startup.lua` › a startup dashboard, mini.starter (9 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A6-restart | `plugin/aineo.lua` | `    return arrangement(config, current_claude_terminal(config))` → `    return arrangement(config, started_claude_terminal(config))` | `tests/test_entry.lua` › :Aineo report, input and claude, :Aineo report after Claude has exited and its terminal was wiped (14 cases) | killed: 3 failing, 3 by assertion, 0 otherwise |
| R-A6-valid | `plugin/aineo.lua` | `  if claude_terminal and vim.api.nvim_buf_is_valid(claude_terminal) then` → `  if claude_terminal then` | `tests/test_entry.lua` › :Aineo report, input and claude, :Aineo report after Claude has exited and its terminal was wiped (14 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A6-eager | `plugin/aineo.lua` | `  require('aineo.layout').focus(role, function()⏎    return arrangement(config, current_claude_terminal(config))⏎  end)` → `  require('aineo.layout').focus(role, arrangement(config, current_claude_terminal(config)))` | `tests/test_entry.lua` › :Aineo report, input and claude, :Aineo report after Claude has exited and its terminal was wiped (14 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A6-layout-eager | `lua/aineo/layout/init.lua` | `  if not has_window(role) then⏎    M.open(type(arrangement) == 'function' and arrangement() or arrangement)⏎  end` → `  local built = type(arrangement) == 'function' and arrangement() or arrangement⏎  if not has_window(role) then⏎    M.open(built)⏎  end` | `tests/test_layout.lua` › focus() with its window open never calls the function it is given for the arrangement (1 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A6-layout-call | `lua/aineo/layout/init.lua` | `    M.open(type(arrangement) == 'function' and arrangement() or arrangement)` → `    M.open(arrangement)` | `tests/test_layout.lua` › focus() with its window closed opens the layout with the arrangement a function returns (1 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |
| R-A10-setup | `plugin/aineo.lua` | `  return (config.resolve_config(vim.g.aineo, config.recorded_setup_options()))⏎` → `  return (config.resolve_config(vim.g.aineo, nil))⏎` | `tests/test_entry_startup.lua` › a bare interactive start (3 cases) | killed: 1 failing, 1 by assertion, 0 otherwise |

**Tally (corrected by the correction, re-measure finding 3):** 35 mutant runs of 32 distinct literal edits, on `b50c145` — R-A1-framing and R-A1-framing-start, R-A2-wiped and R-A2-wiped-start, R-A2-filetype and R-A2-filetype-start are each one edit run against two test files: 31 distinct edits killed by an assertion in all their runs, none by a crash; one survivor, A-give-once, run on the whole suite too (its equivalence restated above). This line first said "35 mutants, 35 distinct literal edits". The brief's M19–M23 on this head: **M19** killed by *headless* alone; **M20** (now `if not has_global_mapping(keys) then` → `if true then`) by *of a key sequence stays*; **M21** by all 4 report cases; **M22** by *in-aineos-claude* alone; **M23** (now wrapping `open_unless_session_restored()`) by 8 of 9 — every dashboard row and *mini.starter gives way*, not *mini.starter stays on screen when autostart is off*, where nothing opens either way. The integrity reviewer's eight whole-suite survivors — PX-expr, PX-script, LOAD-AUTOSTART, CLOCK-UTC, X-MODE, GUARD-STATUS, UNKNOWN-echo, USAGE-extra — each run as its literal edit, each killed by its adopted probe. A first pass on `b5e594c` had R-A9-trim survive (the trim was dead; removed) and R-A6-layout-call killed by the refused arrangement's error, a crash (the case now expects no error); both are fixed in `b50c145`, and the table is the second pass, on the final code.

**Suites on `b50c145`:** `make test` → 603 cases, `Fails (0)`, exit 0, 06:03:53–06:11:06 CEST (433 s) — 573 plus 30 new; `make lint` clean (StyLua check, selene 0 errors, 0 warnings); the deep-require check prints the same 18 intra-home lines (`claude` 3, `layout` 1, `mcp` 7, `report` 7), none from `plugin/`, `tests/` or `scripts/`.

## Correction — the re-measure of `1740e8e`

The re-measure (`neovim-claude-code-reviewer`) confirmed findings 1–8 on `1740e8e`; a third implementer agent took the branch over for one bounded correction. **Boundary:** the fix round's, plus `lua/aineo/claude/` and new cases in `tests/test_claude.lua` for finding 8 only (the orchestrator's widening); every existing Claude-session case stays as it was and green.

**What changed, by the re-measure's finding** — each fix is the re-measure's measured edit, adopted red-first:

- **Finding 1 — fixed (`91208ee`).** `is_file_to_keep()` keeps a file wiped once hidden while it holds changes, which Neovim refuses to wipe (E37): `\o` and the autostart no longer close the user's windows and fail, leaving Claude started hidden. Pin: *open() › keeps a changed file that is wiped once hidden, in the file column* (with a second window open). The dashboards still give way (their pins unchanged and green).
- **Finding 2 — fixed (`91208ee`).** `has_global_mapping()` compares `lhsrawalt` as well as `lhsraw`. Pin: *the user's own mapping › of a key sequence with a Ctrl key in the prefix stays, and the other keys are mapped* (`prefix = '<C-a>'`, a user's `<C-a>s`).
- **Finding 3 — records corrected.** "35 distinct literal edits" was 32: the fix-round tally above now says 35 runs of 32 distinct edits, and the pull request body and this correction's records commit say what `1740e8e` got wrong.
- **Finding 4 — fixed (`91208ee`).** `-E` joins the refused short options (`^%-%a*[cSesE]`); reading 5 updated. Pin: the *improved-ex-mode* row of *a start that is not bare and interactive*.
- **Finding 5 — recorded; the flag is kept.** A-give-once is equivalent on every path short of reloading `aineo.report` (the re-measure's separating input: `package.loaded['aineo.report'] = nil` between two Opens). No user path reloads a module; the re-measure's whole-suite run and this correction's (613 cases, `Fails (0)`) show no test sees the difference. Kept rather than dropped: dropping it would hand the report home a new `getcwd()` at every Open and leave reading 3 true only because the report home ignores it once the Report exists.
- **Finding 6 — fixed (`91208ee`).** `is_unnamed_and_empty()` is keyed on `'bufhidden' ~= 'wipe'` instead of "no filetype": an empty startup buffer the user's configuration gave a filetype becomes Input again, and a dashboard's empty buffer still does not. Pin: *open() › turns the empty buffer Neovim starts with into Input when the user gave it a filetype*.
- **Finding 7 — fixed (`91208ee`).** `layout.focus()` calls the function before `open()`, so a `nil` or `false` it returns is named. Pin: *focus() with its window closed names what the function it is given returned* (`nil` → `got nil`, `false` → `got boolean`).
- **Finding 8 — fixed in the Claude home (`91208ee`), with a limit.** `launch()` checks `executable()` before creating the terminal and raises `claude.cmd: '<word>' is not executable` — 45 characters with `aineo: ` for `claude`, where `jobstart()`'s E475 line was 71. Pins: *start_session() › names claude.cmd and the command that is not executable, and nothing else* (`tests/test_claude.lua`), and *a bare interactive start › with a command that is not executable tells the user without a prompt at 80 columns* (an 80×24 terminal, `claude.cmd = { 'claudx' }`). **Limit:** a command name over 29 characters — an absolute path — still exceeds `v:echospace` (68) at 80 columns and prompts (arithmetic from the re-measure's measured 68; reading 2, *Limits*). The two existing expectations of `jobstart()`'s E475 line (`tests/test_entry.lua`, `tests/test_entry_startup.lua`) now expect the new line. The re-measure wrote the line as 44 characters; it is 45.
- **Found by this correction's mutants (`bb1b3ff`).** Finding 8 removed the only input the fix round's A1 pins went through, so R-A1-framing, R-A1-framing-start and R-A1-first-line survived their groups on `91208ee`. Two real inputs still reach `error_line()`, each measured with a probe first: `\o` on a screen with no room (the layout's split raises `Vim:E36: Not enough room`) and a user's `TermOpen` autocommand that fails (a multi-line `Error executing lua callback` error through `jobstart()`). Both are now `:Aineo open` cases.
- **Test integrity (the re-measure's note) — fixed (`91208ee`).** *focus() with its window closed opens the layout with the arrangement a function returns* catches the error in the child and asserts on the windows; R-A6-layout-call's edit now dies by assertion on what the user sees.
- **Records — the orchestrator's reading.** `prefix = ''` refused: reading 15, routed to the user as the orchestrator's reading of D13, not a change of the row.

**The unit list**, in order: finding 1 in the layout; finding 6 in the layout; finding 7 (`focus()` with `nil`, `false`); the R-A6-layout-call case rewritten; finding 2 (a `<C-a>` prefix); finding 4 (`-E`); finding 8 in the Claude home, then at 80 columns; then, found by the mutants, the two framing pins.

**Seen red — 8 cases**, each run before its own fix was written (the earlier units' fixes in place, the rest of the code as `1740e8e` left it):
- *open() › keeps a changed file that is wiped once hidden, in the file column* — Left 1 Right 4 (the user's second window closed, then E37).
- *open() › turns the empty buffer Neovim starts with into Input when the user gave it a filetype* — Left 4 Right 1.
- *focus() with its window closed names what the function it is given returned › when it returns* `nil` and `false` — each "Observed error: … arrangement: expected table, got function" (one test, two parameters).
- *the user's own mapping › of a key sequence with a Ctrl key in the prefix stays …* — Left `"<Plug>(aineo-send)"` Right `"<Cmd>let g:mine = 1<CR>"`.
- *a start that is not bare and interactive › starts no Claude* + *improved-ex-mode* — Left 1 Right 0.
- *start_session() › names claude.cmd and the command that is not executable, and nothing else* — "Observed error: Error executing lua: Error executing lua: Vim:E475: Invalid value for argument cmd: 'aineo-no-such-claude' is not executable".
- *a bare interactive start › with a command that is not executable tells the user without a prompt at 80 columns* — run with the Claude home held at `1740e8e`'s file: Left `{ blocking = true, mode = "r" }` (the hit-enter prompt). The updated 240-column expectation went red in the same run (Left the E475 line).

**Arrived green — 2 new cases, and 1 rewritten**, each pinning code written before its test, each killer run on the final code: *:Aineo open › on a screen with no room tells the user Neovim's error without its Vim: mark* (R-A1-framing); *› when a TermOpen autocommand of the user fails tells the user the first line of its error* (R-A1-framing, R-A1-first-line); the rewritten R-A6-layout-call case (R-A6-layout-call-adapted, by assertion).

**Mutants** — the list is `t7c_mutant_list.py` (scratchpad): the re-measure's 35 rows, the 10 whose literal no longer occurs adapted to the corrected code (`-adapted`), and 8 of this correction's (C1–C5 and C7, one per numbered item that changes code; C7 run against three files). Each is one literal edit applied to a pristine file, run against a copy of its test file narrowed to its group, and restored byte for byte (`t7c-mutants.py`). Every row ran on code byte-identical to `bb1b3ff`'s; the rows targeting `tests/test_entry.lua` were re-run on `bb1b3ff` after its two new cases.

| Row | Edit (old → new) | Group | Result |
|---|---|---|---|
| M19–M23 | as the fix round's | as the fix round's | killed by assertion: M19 by *headless*; M20 by both user's-own-mapping key cases; M21 by the 4 report cases; M22 by *in-aineos-claude*; M23 by 8 of 9 |
| PX-expr, PX-script, LOAD-AUTOSTART, CLOCK-UTC, X-MODE, GUARD-STATUS, UNKNOWN-echo, USAGE-extra | the integrity reviewer's literals | as the fix round's | each killed by assertion, by its adopted probe |
| A-give-once | `  if report_environment_given then` → `  if false then` | the report tool (4); **whole suite** | survived both: whole suite 613 cases, `Fails (0)`, exit 0 (finding 5) |
| R-A1-framing | `local ERROR_FRAMING = { '^Error executing lua: ', '^.-%.lua:%d+: ', '^Vim:' }` → `{ '^.-%.lua:%d+: ' }` | :Aineo open (12) | killed 2, assertion (both new cases); survived on `91208ee` |
| R-A1-framing-start | the same edit | a bare interactive start (4); **whole suite** | survived its group; whole suite: 613 cases, 2 fails, both by assertion (the two new cases) |
| R-A1-first-line | `  local line = message:match('^[^\n]*')` → `  local line = message` | :Aineo open (12) | killed 1, assertion (*TermOpen*); survived on `91208ee` |
| R-A8-empty, R-A9-bar, R-A5-global, R-A4-session, R-A6-restart, R-A6-valid, R-A6-eager, R-A10-setup | as the fix round's | as the fix round's | each killed by assertion |
| R-A2-wiped-adapted / -start-adapted | `  return is_file(buffer) and (vim.bo[buffer].bufhidden ~= 'wipe' or vim.bo[buffer].modified)` → `  return is_file(buffer)` | open() / dashboards | killed 1 / 3, assertion |
| R-A2-filetype-adapted / -start-adapted | `    and vim.bo[buffer].bufhidden ~= 'wipe'⏎` → *(removed)* | open() / dashboards | killed 1 / 1, assertion |
| R-A3-bundle-, R-A3-attached-, R-A7-e-, R-A7-s-adapted | `'^%-%a*[cSesE]'` → `'^%-[cSesE]'` / `'^%-%a*[cSesE]$'` / `'^%-%a*[cSsE]'` / `'^%-%a*[cSeE]'` | not bare (14) | killed 1 each, assertion |
| R-A6-layout-eager-adapted | the arrangement built before `has_window()` | its one case | killed 1, assertion |
| R-A6-layout-call-adapted | the three lines calling the function removed, leaving `    M.open(arrangement)` | its one case | killed 1, assertion (the windows) |
| C1-keep-changed | the finding-1 guard → `  return is_file(buffer) and vim.bo[buffer].bufhidden ~= 'wipe'` | open() | killed 1, assertion |
| C2-ctrl | `    return mapping.lhsraw == typed or mapping.lhsrawalt == typed` → `    return mapping.lhsraw == typed` | the user's own mapping | killed 1, assertion |
| C3-empty-filetype | `    and vim.bo[buffer].bufhidden ~= 'wipe'⏎` → `    and vim.bo[buffer].filetype == ''⏎` | open() | killed 1, assertion |
| C4-E | `'^%-%a*[cSesE]'` → `'^%-%a*[cSes]'` | not bare (14) | killed 1 (*improved-ex-mode*), assertion |
| C5-fn | the three lines → `    M.open(type(arrangement) == 'function' and arrangement() or arrangement)` | the nil/false case (2) | killed 2, assertion |
| C7-exec / -80 / -open | `  ensure_executable(settings.cmd[1])⏎` → *(removed)* | start_session() / a bare interactive start (4) / :Aineo open (12) | killed 1 / 2 / 1, assertion; the 80-column kill 3 of 3 runs |

**Tally:** 58 mutant runs of 38 distinct literal edits (43 rows; R-A1-framing and -start, R-A2-wiped-adapted and -start, R-A2-filetype-adapted and -start, and C7-exec, -80 and -open each share one edit). 37 distinct edits killed by an assertion, none by a crash; 1 survivor, A-give-once (finding 5). The runs: 42 in the first pass on `91208ee`, 3 error-line rows and 8 `tests/test_entry.lua` rows again on `bb1b3ff`, C7-exec-80 twice more, C7-exec-open on `bb1b3ff`, and 2 whole-suite runs.

**Suites on `bb1b3ff`:** `make test` → 613 cases, `Fails (0)`, exit 0 — 603 plus 10 new (8 seen red, 2 arrived green); `make lint` clean (StyLua check, selene 0 errors, 0 warnings); the deep-require check prints the same 18 intra-home lines.

## Open threads

- **Isolation documents (`ai/`, the orchestrator's):** the root `CLAUDE.md`, `neovim-lua-developer.md` and `neovim-claude-code-integrator.md` do not mention the `PATH` guard, the `AINEO_CHILD` removal or the autostart preset (records review R1).
- **An error line longer than `v:echospace`** still prompts at a bare start (68 columns of an 80-column screen). The correction shortened the not-executable line in the Claude home (45 characters for `claude`); a command name over 29 characters, or any other long error, still prompts.
- **The terminal of the last session is kept twice** — by the claude home and by the composition root (`claude_terminal`), since the claude home exports no query for it. A `session_buffer()` on `aineo.claude` would let the composition root ask instead; outside this packet.
- **`tests/helpers/entry_editor.lua`** can stall instead of failing when the editor blocks between its mode check and its request (`launch()`, `mode()` and `wait_for_screen()`, added by the correction, let the 80-column pin avoid it by asking nothing that waits; `start()` and `settle()` still can); the startup suite's patience is 10 s per wait (`PATIENCE_MS`), each UI case about 1.5–3 s.
- **`tests/test_mcp_blocked_editor.lua` (T5)** writes a file `v:null` into the checkout's root when its editor autostarts and its records file is elsewhere — latent while the suites' preset holds, not only a past event of one run.
- **Learning candidates** for the orchestrator's knowledge pass: an RPC request to a Neovim at a hit-enter prompt never returns — check `nvim_get_mode().blocking` first, and even then a request can race the prompt; in the TUI the server's `v:argv` is `nvim --embed <user args>`, and `-c`/`+` commands run before `VimEnter`; a suite's interactive editor is a bare start and autostarts unless the suite presets `autostart`; a startup message longer than `v:echospace` holds Neovim at a hit-enter prompt; with `bar`, a user command's argument loses its trailing space.
- **Records corrected by this round** (a commit cannot be rewritten once pushed): `5e2d6c4`'s subject says "three crash kills" — two mutants were killed by a crash and a third survived, as its body says; `ed1eeba`'s subject is 72 characters, one over the limit; `6476e44` says the fake "replays Claude Code 2.1.281's recorded messages" — its handshake is recorded, its report-tool call reconstructed.

## Task lines

- [X] T7 — Entry point (C1): prefix mapping, `<Plug>` mappings, `:Aineo`, autostart — the composition root in `plugin/aineo.lua`; prefix at `VimEnter` (lazy.nvim read at v11.17.5); autostart under D3 with `-c`, `+`, `-S` refused, and in the fix round `-e`, `-s`, their bundled spellings and a plugin-restored session (readings 5, 14); dashboards given way to two scheduled callbacks after `VimEnter` (D15); the fake's `mcp-client` mode; the suites' `PATH` guard, `AINEO_CHILD` removal and autostart default. Fix round: A1–A10, I1–I7 and R2–R12 answered (*Fix round*); dashboard-nvim gives way through two layout checks (D15), only `\o` restarts (R1), `-c`/`-S`/`-e`/`-s` in any spelling and a plugin-restored session refuse the autostart. Correction (re-measure of `1740e8e`): findings 1, 2, 4, 6, 7 and 8 fixed (8 in the Claude home, a name over 29 characters still prompts at 80 columns), 3 and 5 recorded, the error line re-pinned, `prefix = ''` routed as the orchestrator's reading (reading 15) (*Correction*). Open: the isolation documents (an `ai/` pass, R1); see *Open threads*.

## Commits

Merged by rebase into `dev` on 2026-09-25 (PR #17, final head `a036ada`; 12 commits; per-file identity 26 of 26), recorded by the orchestrator's knowledge pass:

| `dev` | was | round |
|---|---|---|
| `fa6b28c` | `ed1eeba` | packet — Guard the suites against the real claude and refuse self-containing opts |
| `3f4a365` | `e6462d5` | packet — Wire :Aineo, the <Plug> mappings and the prefix to the homes |
| `9be0f36` | `6476e44` | packet — Start aineo by itself on a bare nvim, after any startup dashboard |
| `36113d3` | `5e2d6c4` | packet — Turn three crash kills into assertions and pin the report clock |
| `46eefbe` | `5dc3fc0` | packet — Record the T7 entry point session |
| `cbc451f` | `7774724` | packet — Name pull request #17 in the T7 session note |
| `cf88418` | `b5e594c` | fix round — Answer the attack and integrity findings on the entry point |
| `cdbd1a2` | `b50c145` | fix round — Drop the redundant trim and make a layout kill an assertion |
| `b5c935f` | `1740e8e` | fix round — Record the T7 fix round and correct the packet's records |
| `151221a` | `91208ee` | correction — Correct the entry point's regressions the re-measure found |
| `4956301` | `bb1b3ff` | correction — Pin the error line's framing through inputs that still reach it |
| `201873b` | `a036ada` | correction — Record the T7 correction and the counts 1740e8e got wrong |
