# 2026-09-24 — T3 layout

**Author:** Mathias Santos de Brito, with Claude — implementer agent (`neovim-lua-developer`)
**Branch:** `feature/t3-layout` · **Pull request:** #9 (packet round, one fix round, then a correction by a second implementer agent)

## Links

- **Project:** [[Projects/aineo]]
- **Plan:** [[Planning/aineo — v1 agent console]] — T3, C2, C9, D4, D5, D6, D7, D13, F3, A5
- **Wave:** [[Implementation/Waves/00002-layout-session-report/plan]] · the brief `Implementation/Waves/00002-layout-session-report/brief-t3-layout.md` · its review `brief-review.md`, findings 1, 2, 11 and 12
- **Neighbours in this wave:** T4's brief, S6 (a restart puts the new terminal in the old one's windows); T5's brief, RP3 (the Report buffer is named from creation)
- **Reviews of PR #9, at `6e9aba0`:** attack (findings A1–A13), test integrity (I1–I8) and records (its findings 1–10, written "records finding N" here so as not to collide with the plan's risk rows), and the orchestrator's fix-round brief (its decisions 1–15). The durable record of all of them is PR #9's body.
- **The re-measure of PR #9, at `977d727`:** its findings 1–11, written "re-measure finding N" here, and the orchestrator's correction brief, whose items 1–8 answer them. PR #9's body has a *Correction* section.

## Context

**Goal.** T3 builds aineo's screen:
- C2 — the three windows, pinned against resizing, restored by `\o`;
- C9 — the file column, and the `BufWinEnter` redirect that fills it.

The entry point (T7) wires T3 to the prefix keys and to startup. Until then, nothing calls the layout.

## What was done

**Two files make up the home.**
- `lua/aineo/layout/init.lua` holds the state and the entry point.
- `lua/aineo/layout/columns.lua` reads `winlayout()`'s tree. It was split out in the fix round, and only `init.lua` requires it.

**The entry point is `require('aineo.layout')`:**

- **`open(arrangement)`**
  - **What it is handed:** `arrangement` is `{ claude, report, report_height }`.
  - **The first call builds the layout.**
    - Claude's buffer goes on the left, at half the columns (D4).
    - The Report goes above Input on the right, at `report_height` of that column (D13).
    - The cursor lands in Input.
  - **What happens to the tab.**
    - Its other windows close and their buffers stay loaded; floating windows are left alone.
    - A file shown in the current window stays, as the file column.
    - Called from a float, `open()` builds from the tab's first non-floating window.
  - **Input.**
    - It is made once: from a buffer already named `aineo://input`, such as a restored session's, else from the unnamed, empty startup buffer, or else as a new buffer.
    - It is named `aineo://input`, set to `nofile`, unlisted, `bufhidden=hide`, with no swap file.
    - It is made a scratch buffer again whenever it is shown, and made anew after `:bwipeout`, again from a buffer named `aineo://input` when there is one.
  - **Later calls restore.** While any of the three windows exists, a call works in the layout's tab, moving there when another tab is current. It:
    - reopens only the closed windows, in their places;
    - shows each window its buffer (the handed ones);
    - puts the proportions back.
  - **Its arguments are checked** with `vim.validate`, and nothing changes before the check.
- **`focus(role, arrangement)`**
  - Moves the cursor to the Claude, Report or Input window.
  - When that window is gone, it reopens the layout first.
  - With the window present, it opens nothing and resizes nothing.
- **`input_buffer()`**: returns the Input buffer, for T6.

**Behind the entry point.**

- **Pins:** `'winfixwidth'` and `'winfixheight'` on the three windows.
- **Proportions:** put back after `WinClosed` (scheduled), `VimResized` and `TabEnter`, while the three windows exist.
- **The redirect.** A file (empty `'buftype'`) shown in one of the three windows schedules a move:
  - The file goes to the file column, which is created on first use and reused after. It is created right of Claude's column, or at the left of the tab when Claude's window is closed.
  - Only a window of the file column that shows a file, is not one of the layout's and has no `'winfixbuf'` is reused; when there is none, a new file column opens.
  - The window gets its buffer back.
  - The cursor lands in the file column, at the position the command gave it.
  - A changed buffer in the file column that leaving would lose is not abandoned: the file opens above it. That is under `'nohidden'`, or with `'bufhidden'` `wipe`, `delete` or `unload`, unless another window shows the buffer too; `'bufhidden'` `hide` always lets it leave.
  - When there is no room above it, the file stays in the window it was opened in, and one warning names it.
  - A window's own buffer is never taken for a file.
  - The move checks, when it runs, that the captured window is still one of the layout's and still shows the file.
- **The file column** is the set of columns between Claude's and the right one, in the deepest row of the layout's tab that holds the layout's windows. When one side is closed, the side that remains bounds it. With the file column open, the three columns take equal thirds (D6).

**Tests.** 131 cases in five files after the correction (110 after the fix round), plus a helper:
- `tests/test_layout.lua` — 35 cases (33);
- `tests/test_layout_file_column.lua` — 57 (41);
- `tests/test_layout_input.lua` — 9 (6);
- `tests/test_layout_proportions.lua` — 25;
- `tests/test_layout_tabs.lua` — 5;
- `tests/helpers/layout.lua` — the child at 80×24, the stand-ins (a terminal running `cat`, a scratch buffer named `aineo://report`), boxes, the one-cell tolerance, and `wait_until()` for typed keys.

**Results.**
- **The correction:** `make test` at `5e52956` gave 242 cases, `Fails (0)`, rc 0, in 91 s.
  - That is T1's 111 cases plus the layout's 131: 35, 57, 9, 25 and 5 by file, in the order listed above.
  - `5e52956` has the final head's code and tests. The commit after it changes only this note.
  - The output, with the rc and the duration, is in the scratch file `t3c-suite-5e52956.txt`.
  - `make lint` is clean, and the deep-require check prints the same single in-home line.
- **The fix round:** `make test` at `ee76e5b` gave 221 cases, `Fails (0)`, rc 0, in 91 s.
  - That is T1's 111 cases plus the layout's 110.
  - `ee76e5b` had the fix round's final code; `977d727` added only this note.
  - The output is in the scratch file `t3-fr-suite-ee76e5b.txt`. That file holds the 221 cases and `Fails (0)`, but not the rc or the 91 s, which were printed only to stdout (re-measure finding 8).
  - Re-measured at `977d727`: the re-measure got 221, `Fails (0)`, rc 0 in 1:46.72. The correction got 221, `Fails (0)`, rc 0 in 91 s, with the rc and duration kept in the scratch file `t3c-suite-977d727.txt`.
- **The packet round:** 185 cases, `Fails (0)`, rc 0, both times it was run.
  - At `d4eafd5` it took 87 s, measured in the session (the tool printed `d4eafd5` and then `rc=0 seconds=87`). Its output file was overwritten later, so records finding 5 could not re-check it.
  - At `6e9aba0` it took 89 s.
  - The baseline was 111 cases in 88 s at `8725632`.
- `make lint` is clean.
- The deep-require check prints one line, `lua/aineo/layout/init.lua:7: local columns = require('aineo.layout.columns')`. That is a require inside its own home, which is allowed.

## Unit list

### Packet round

This was the slicing planned before the first test, in order, by the brief's L-IDs.
- **L1:**
  1. three windows in place;
  2. Claude at half;
  3. the Report's share;
  4. the cursor in Input.
- **L2:**
  5. the startup buffer becomes Input;
  6. otherwise a new Input;
  7. Input is a named scratch buffer.
- **L3:**
  8. other windows closed, their buffers loaded.
- **L7:**
  9. a file goes to a new column between Claude's and the right one, with the cursor in it (per window);
  10. the window keeps its own buffer (per window);
  11. `:edit` in the empty Input;
  12. `:buffer` and a quickfix jump;
  13. the line a command asks for is kept.
- **L8:**
  14. equal thirds.
- **L9:**
  15. a second file reuses the column;
  16. a file opened inside the column stays.
- **L10:**
  17. `:q` gives back D4.
- **L3:**
  18. a file in the current window stays, as the file column.
- **L4:**
  19. `:wincmd =`;
  20. split and close in Input;
  21. D6 with a new window closed.
- **L5:**
  22. no new window;
  23. proportions back;
  24. closed windows come back;
  25. the buffers handed are shown.
- **L6:**
  26. `VimResized`.
- **L11:**
  27. buffers that are not files are left alone.
- **L12:**
  28. focus;
  29. focus reopens.
- **L13:**
  30. inert when closed.
- **Last:**
  31. the arguments are checked.

**Added while working:**
- the file column split in two;
- closing one of the three windows raises no error;
- the cursor after `open` from a file;
- Input shown again, and a restarted Claude buffer given back;
- the move re-checks the window and the file;
- Input kept after all three windows closed;
- two more L4 splits in D4, `:topleft vnew` and `:vertical botright new`, from the brief review's P3d table.

### Fix round

This was the slicing planned before the first change, in the order the orchestrator set.

**Phase A: the test-integrity reviewer's measured tests.** Each was adopted as theirs and seen red under their literal mutant on the unchanged code:
1. F3;
2. F3b;
3. F1;
4. F2;
5. F5;
6. F6;
7. F7;
8. F4;
9. the I8 pin.

**Phase B: code, test first.**

10. A1 — a full-width window, from `:botright 6new` and from `:help`.
11. A2 — Input deleted by `:bdelete`, then shown again. Three tests follow from it: a file opened afterwards, `:qa`, and a bare `:edit`. A fourth covers a window's own buffer shown again (the Report).
12. A4 — Input wiped, then `open()` and then `focus()`.
13. A8 — a failed open, then another.
14. A9 — a file opened in a window that remains, in three states, with the L13 test rewritten.
15. Decision 10 — the stale captured window.
16. A3 — five tabs cases:
    - restore from another tab;
    - the layout's tab shown;
    - a file after that restore;
    - focus from another tab;
    - a resize while another tab is shown.
17. A7 — under `'nohidden'`, a changed file, and then that same file again.
18. A11 — floats left alone, and `open()` from a float.
19. I6 — a typed-keys test that waits for the move.

**Refactor.** The window-tree reading moved to `columns.lua`.

**Added after the first full mutant run of the round.** That run, at `7ec4647`, left four of my own mutants alive, so four pins were added, each killed by its mutant:
- where A9's file column opens with Claude's window closed;
- that it opens in the layout's tab when another tab is shown;
- the Report closed with a file opened from Input (a fourth A9 parameter);
- `'hidden'` with a changed file in the file column.

### Correction

A second implementer agent took the branch over at `977d727` for the re-measure's findings. It kept the fix round's decisions. This was the slicing stated before the first test.

**Phase 1: pins on the head's lines.** These change no code; each pins code already there and is killed by the re-measure's literal mutant.
1. P1 — a window across the bottom and one down the left; a second file still goes to the file column (re-measure finding 6).
2. P2 — `'nohidden'`, a file column holding no change takes the next file in place (finding 6).
3. P3 — the "above it" case checks that the new window sits directly above the changed one (finding 6).
4. P4 — from a float over two files, the first tiled window's file stays (finding 6).
5. The tabs case "leaves that tab alone" enters Input's window by id (finding 4).
6. E2 — a user's `BufEnter term://* startinsert` leaves Normal mode after `open()` (finding 5).
7. E3 — a file shown in a window before it became Input stays when shown again under `noautocmd` (finding 5).

**Phase 2: fixes, test first.**

8. P7, over both sides — a sidebar at the edge of the tab keeps its buffer (finding 1).
9. P9 — with Claude's window closed and Input moved left of the Report, the file is shown (finding 1).
10. P8 — a `'winfixbuf'` window in the file column's place takes no file and raises no error (finding 1).
11. P6, over `wipe`, `delete` and `unload` — no E37 under `'hidden'` (finding 2).
12. `'bufhidden'` `hide`, under `'hidden'` and `'nohidden'` — the next file in place (finding 2, the measured fix's third clause).
13. A changed buffer shown in two windows of the file column, under `'nohidden'` — the next file in place (finding 2, its second clause).
14. No room above changed files, under `'nohidden'` — no raw E36; the file stays and one warning names it (finding 2).
15. P5 — a loaded buffer named `aineo://input` before the first `open()` becomes Input (finding 3).
16. The same after Input was wiped, on the restore path (finding 3).

**Added after the correction's mutant runs.** Its fixes had masked the kills of mutants that its tests used to catch, so pins were added.
- The first run, at `818cc26`, left one of the re-measure's own mutants alive, r9-past-claude-always-faithful:

  17. A window left of Claude is not the file column, so opening again keeps Claude at half the columns (the counterpart of F6).

- The fix round's 108 rows, re-run at `db3d7ca`, left three that `ee76e5b` had killed. Two are pinned, and the third is equivalent on this code (see *Mutants*):

  18. With the Report's window closed, a file window right of Input keeps its file (fr-right-from-input-only).
  19. Input renamed with `:file` stays Input, with its text, through a full close (m-second-input@fr).

## Decisions & reasoning

**The orchestrator's readings, as briefed:**
- L1 — the cursor ends in Input.
- L3 — the tab's other windows close.
- L4 — re-apply on `WinClosed`.
- L6 — re-apply on `VimResized`.
- L11 — non-files are left alone.
- L12 — focus reopens.

**The orchestrator's fix-round decisions.** Numbers 1–15 are the brief's.
- **3: the layout lives in one tab.**
  - `open()` and `focus()` from another tab go to the layout's tab.
  - The layout's proportions come back when its tab is entered.
- **8: the layout counts as closed only when none of its three windows remains.** That is the L13 reading now in force.

**Mine, renumbered this round as LR1–LR10.**

The packet round had called LR1–LR6 `R1`–`R6`, which collide with the plan's risk rows R1–R3 (records findings 1 and 2).
- Commit `6e9aba0` used `R1`–`R6` for LR1–LR6, in this order. Commit `c1c1482`'s "(R1, T4's S6)" names the plan's risk R1, and stays so.
- The packet round's PR body numbered them differently: R2–R6 there are LR3, LR4, LR5, LR6 (the arrangement) and LR6 (`report_height`). It had no entry for LR2.
- Those labels are withdrawn, not reused. This list is the one numbering.

- **LR1 — the redirect is scheduled, not synchronous.**
  - A spike (Nvim 0.11.6, a mini.test child) put the move inside `BufWinEnter`. It left the cursor in the aineo window, and applied the cursor step of `:edit +3` and of `:cfirst` there.
  - The packet round kept no output of that spike (records finding 10). It was re-run on 2026-09-24 for this round, and its output is in the scratch file `t3-spike/s2-output.txt`. The lines that matter:
    - `":edit +3" … "sched=false" … "cur=input" … files={ buf = "f1.txt", cursor = { 1, 0 } }`
    - `"qf jump" … "sched=false" … "cur=input" …`
  - *Rejected:*
    - `'winfixbuf'` (A5, F3), which refuses where D7 redirects;
    - `BufReadPost`, which a loaded file shown with `:buffer` never fires (a mutant measures this).
- **LR2 — the file column is found structurally, from `winlayout()`.** It is read in the deepest row that holds the layout's windows (A1).
- **LR3 — `\o` restores, and leaves the cursor where it is in the layout's tab.**
- **LR4 — only an *empty* unnamed buffer becomes Input.**
- **LR5 — Input is unlisted and kept when hidden.**
- **LR6 — `focus` takes the arrangement.** `report_height` has no default in the layout; T7 resolves it.
- **LR7 — under `'nohidden'`, the next file opens above a changed buffer in the file column.** The other choice was refusing with one message. Showing the file loses nothing, while refusing would.
- **LR8 — floats are left alone.** From a float, the layout is built from the tab's first non-floating window, rather than refused.
- **LR9 — where a file column opens when some of the three windows are closed.**
  - With Claude's window closed: at the left of the layout's tab.
  - With the right column closed: right of Claude's.
- **LR10 — a window's own buffer is never redirected out of it.** A Report deleted and shown again had come back with an empty `'buftype'` and moved to a fourth window.

**The orchestrator's correction decisions, items 1–8 of its brief.**
- **1–3: fix re-measure findings 1, 2 and 3**, adopting the re-measure's measured fixes red-first.
- **4–7: correct the test and the records** named by findings 4–8.
- **8: record findings 9, 10 and 11 as limits, not fixed.** They were present at `6e9aba0` and are outside the correction.

**What the correction decided inside those items.**
- **LR9, narrowed.** Where LR9 reads "the columns left of the right one", only a window that shows a file, is not one of the layout's and has no `'winfixbuf'` is reused. Anything else in that place is passed over, and a new file column opens.
- **LR11 — at the file column's limit, the file stays where it was opened, with one warning.** Under `'nohidden'`, a column full of changed files has no room for another window above them (E36). The re-measure left this unbuilt.
  - The file stays in the aineo window it was opened in. `vim.notify` warns once, at `WARN`: `aineo: the file column has no room for <name>, which stays where it was opened`.
  - *Rejected:*
    - a new file column, which multiplies columns (re-measure finding 9's class);
    - giving the aineo window its buffer back, which leaves the file shown nowhere (the failure `fr-right-from-input-only` pinned).
  - Only E36 is caught. Any other error from that split is still raised. The only other one known is re-measure finding 11's E11, a limit.

### Readings for the MVP review

These are readings the user never chose, with how to see each:

1. **LR1 — the redirect is scheduled.** Keys typed or mapped after `:edit` act in the aineo window before the move, and a mapping's `:tabnew` is pulled back (A12). Reproduce with `nnoremap <F2> :e a.txt<CR>:tabnew<CR>`.
2. **LR3 — `\o` leaves the cursor in place, and from another tab moves to the layout's tab.**
3. **LR4 — text typed into the startup buffer is kept as a file**, and the layout gets a new Input.
4. **LR5 — Input is out of `:ls` and `:bnext`.**
5. **LR7 — under `'nohidden'`, the file column splits** rather than refusing.
6. **LR8 — `\o` from a floating window.**
7. **LR9, and decision 8 — while any aineo window remains, files are redirected.** A file column opens at the left edge of the tab when Claude's window is closed. Since the correction, a sidebar, a `'winfixbuf'` window or a moved aineo window in that place is passed over for a new column.
8. **Decision 3 — the layout lives in one tab.**
9. **A6 — files opened into new windows from an aineo window are left to Neovim.**
   - `:split a.txt`, `:sbuffer`, `<C-w>f` and `:pedit` land in the right column, squeezing the Report or Input.
   - With no file column open, quickfix `<CR>` splits an aineo window or opens full-width.
10. **L11 (the orchestrator's) — help, terminals and scratch buffers in an aineo window stay** until `\o`.
11. **LR11 — under `'nohidden'`, a file column full of changed files leaves the next file where it was opened, with one warning.** Reproduce at `:set lines=5 nohidden`: from Input, open a file and change it, twice, then open a third.
12. **Re-measure finding 9 — moving the layout's windows breaks the file column, and `\o` does not repair it.** After `:wincmd H`, `J`, `K` or `L` on an aineo window, or `:split` in Claude's, no row holds the three windows in their places, so every redirect adds a column. Reproduce with `:wincmd L` in Claude, then three files opened in aineo windows, then `\o` (the re-measure's `remeasure9-s01.lua`).
13. **Re-measure finding 10 — another tab can take Claude's window.** `nvim_win_set_config(claude_window, { split = 'left', win = <a window of tab 2> })` moves it and keeps its id. The next file opened from Input pulls the cursor to tab 2, the file column opens there, and `\o` changes nothing.
14. **Re-measure finding 11 — `:e a.txt<CR>q:` typed as one input raises a raw E11.** The scheduled move runs inside the command-line window, and a.txt stays in Input once `q:` closes.

**Test design.**

- **Geometry tolerance: one cell.** Before-and-after comparisons compare exactly.
- **Tests that act through requests read scheduled results with their next request.**
  - The test-integrity reviewer measured this: 200 of 200 per case, and about 73,000 reads with no miss under 12-way load.
  - Typed keys (`nvim_input`) are not a request. The reviewer measured 200 of 200 reads still seeing the file in Input without a wait.
  - So the helper says a test that types keys waits with `wait_until()`, and the one typed-keys test does.
- **Claude's terminal content is compared as its job channel.**

## Red and green

### Packet round

**47 cases were seen red, each before its code.**
- 34 were assertion failures.
- 13 were errors, and each error was the missing behaviour itself:
  - `module 'aineo.layout' not found` — 1 case;
  - `focus` a nil value — 3 cases;
  - `E95` from a second Input — 6 cases;
  - `Invalid window id` from `focus` entering a closed window — 3 cases.

**27 cases arrived green, each with a killer that was run.**
- 3 by `'equalalways'`.
- 2 by code written ahead of its test: the cursor after `open` from a file, and Input shown again.
- The rest were spent by an earlier unit.

**Test-side slips.** The packet round's PR body said "three test-side errors", its note and report said "five slips", and neither mentioned a sixth change (records finding 3). The full list:
- Five slips, corrected before they were counted:
  - two first runs that crashed rather than failing an assertion (L7, L3);
  - a terminal whose lines changed under the comparison;
  - two argument slips in child code.
- Two tests changed after a mutant run crashed them rather than failing them:
  - "gives a restarted Claude buffer back after a file leaves its window" (U31): its act moved from entering the window by buffer to entering it by id, so that `m-old-claude` kills it by assertion.
  - "keeps Input and its text" (U41): its act was wrapped in `expect.no_error`, so that `E95` is an assertion.

**Written together, which the loop forbids.** Three pairs:
- the handed Claude and Report buffers;
- `VimResized` in D4 and D6;
- the table and non-table arrangements.

### Fix round

**Phase A: 9 of the reviewer's tests, each green on the unchanged code and seen red by assertion under the reviewer's literal mutant.**
- F3: `{16,26,26}`, `{26,15,15}`, `{19,19,19}`;
- F3b: `{14,3}`, `{6,7}`, `{7,6}`;
- F1: Left 4, Right 1, both cases;
- F2: `{false,false}` under `nohidden`;
- F5: 3 windows vs 4;
- F6: 27 vs 54;
- F7: 4 vs 1;
- F4: 3 vs 1;
- I8: Claude 40 vs 20.

**Phase B.**
- **Seen red before their code, 15 cases.** The round therefore saw 24 cases red: 9 in Phase A and 15 here. Its report said "25 … the 16 red before their code", but these rows sum to 15 (re-measure finding 7, corrected by the correction).
  - A1, ×2: 6 windows vs 5;
  - A2: `buftype ""`, `bufhidden ""`, `swapfile true`;
  - own buffer: 4 vs 3;
  - A4: `expect.no_error` with "Invalid buffer id: 1";
  - A9, ×3: 4 vs 3, 4 vs 1, 4 vs 2;
  - decision 10: `v:errmsg` "Invalid window id". This was also reproduced on `6e9aba0`'s code.
  - A3 restore: tab 2 vs 1;
  - A3 resize: Report 14 vs 24;
  - A7: E37 traceback;
  - A7 same file: 5 vs 4;
  - A11: `{false,false}`;
  - A11 from a float: E444.
- **Arrived green, each killer run:**
  - a file after `:bdelete`, `:qa`, and a bare `:edit` (all three killed by fr-no-input-scratch);
  - `focus` after wipe (fr-no-new-input);
  - A8 (fr-input-recorded-late). Its code was written ahead of its test, in the A4 refactor.
  - the tab shown, and the file after a restore from another tab (fr-restore-in-current-tab). Both were said to be red on `6e9aba0`'s code, tab 2 showing `{2,6,4}` where it had shown `{5,4}`.
    - **Corrected (re-measure finding 4).** The second case, "leaves that tab alone", was killed only by a crash at `ee76e5b`. Under the mutant, Input is not in the current tab, and `enter_window_showing` failed with `nvim_set_current_win: Invalid 'window'`. The committed case crashes on `6e9aba0`'s code too; the `{2,6,4}` came from a different arrange.
    - The correction captures Input's window by id before the second tab opens and enters it by id. fr-restore-in-current-tab now fails the case by assertion: `{ 2, 6, 4 }` vs `{ 5, 4 }`.
  - focus from another tab (fr-restore-in-current-tab);
  - the typed-keys test (M11);
  - the four pins added after the first full mutant run, each killed by the mutant that had survived:
    - fr-claude-gone-right;
    - fr-claude-gone-no-enter-tab;
    - fr-right-from-input-only (the fourth A9 parameter, Report closed);
    - fr-hidden-ignored.
- **Changed tests:**
  - The L13 test changed with decision 8. It had closed Claude and the Report and expected no redirect from Input; it now closes all three windows.
  - The first "Report shown again" test arrived green without reaching its path, because `:bdelete` closes the Report's window. It was rewritten to put help in the Report's window first, and then went red.
  - The first "focus from another tab" test used the Report. The Report reopens relative to Input's window, so it reopens in the layout's tab anyway, and the killer did not kill it. The test was rewritten for Claude.
- **Test-side slips, corrected before counting:**
  - a window captured before `:bdelete` closed it;
  - two slips in the float test's child code (two arguments to one call, and a `local` inside an expression);
  - a vararg slip in the helper that crashed every case once.
- **Written together:** two tabs tests, the tab shown and the file after a restore, both of which arrived green.

### Correction

**13 cases were seen red, each before its code, each by assertion.**
- P7, Claude's window closed with a sidebar on the left, and the Report and Input closed with one on the right: 5 vs 4, the sidebar showing the file.
- P9: the file shown in 0 windows vs 1.
- P8: `v:errmsg` held the E1513 traceback.
- P6, `wipe`, `delete` and `unload`: `v:errmsg` held the E37 traceback.
- `hide` under `set nohidden` and under `set hidden`: 5 windows vs 4.
  - The `set hidden` parameter was red only on the intermediate code. P6's minimum clause had dropped it, and it passes on `977d727`'s code.
- A changed buffer shown in two windows under `'nohidden'`: 6 windows vs 5.
- No room above changed files: `v:errmsg` held the raw E36 traceback.
- P5, and the restore path: `Failed expectation for *no* error`, E95.
- **On `977d727`'s code.** Run against `977d727`'s `lua/aineo/layout/`, the final tests fail 12 of these 13, each by assertion; the `set hidden` parameter passes. The output is in the scratch file `t3c-on977-out.txt`.

**8 new cases and 2 changed ones arrived green, each pinning code already there.** Each killer was run and killed its case by assertion. The phase-1 killers ran as literal edits on `7251c71`, whose code is `977d727`'s, and every killer ran again at `5e52956`.
- P1: r9-shallowest-row, 7 vs 6.
- P2: r9-can-leave-hidden-only, 5 vs 4.
- P3 (the changed "above it" case): r9-split-below, 23 vs 0.
- P4: r9-start-last-tiled, 0 vs 1.
- The tabs case, entered by id (changed): fr-restore-in-current-tab, `{ 2, 6, 4 }` vs `{ 5, 4 }`.
- E2: m-enter-claude, `"i"` vs `"n"`.
- E3: m-any-window@fr, 1 vs 5.
- The left-bound pin: r9-past-claude-always-faithful, 26 vs 40.
- A file window right of Input, with the Report closed: fr-right-from-input-only, 5 vs 4.
- Input renamed with `:file`: m-second-input@fr, 4 vs 1.

**Two arranges changed before they counted.**
- The no-room test first had one changed file at 5 lines, and its split fitted. A spike measured that the second split, not the first, is refused at 5 lines, so the test stacks two changed files. Its expected warning was reworded after the red, which was the raw E36 on its first assertion.
- The restore-path test first used `:badd`. Neovim silently wipes that unloaded buffer when the name is taken, so no E95 was reached (5 vs 4 on the Input buffer's number). It now loads the buffer.

## Mutants

### Packet round

- 50 mutants were run at `d4eafd5`, and all 50 were killed by assertion.
- The test-integrity reviewer re-ran them at `6e9aba0`. It reproduced every count, and found 12 more mutants that survived. 9 of those are real gaps, now closed by the adopted tests; 3 are equivalent.
- The claim "each [mutant] pins one line of the home" was not true for the `'winfixwidth'` and `'winfixheight'` lines. The test-integrity reviewer showed that no single window's pin was pinned. That is withdrawn; F3 and F3b pin them now.
- One edit in the packet round's table was ambiguous (records finding 6). `m-open-no-apply` is: in `M.open`, `pin_windows()` / `apply_proportions()` / `watch_windows()` → `pin_windows()` / `watch_windows()`.

### Fix round

The run was at `ee76e5b`, the fix round's final code. Each mutant ran one at a time from a pristine copy, against the five layout files.

- **108 mutants:**
  - 83 killed by assertion;
  - 5 survived, each with its reason below;
  - 20 not run, because their literal text no longer occurs. Each of those has an adapted edit, which was run.
- **The list:**
  - the test-integrity reviewer's 21, as their literal edits;
  - the packet round's 50, as their literal edits;
  - 20 adapted edits;
  - 17 new ones.
- **The first full run was at `7ec4647`.** It left 4 of the new ones alive (fr-claude-gone-right, fr-claude-gone-no-enter-tab, fr-right-from-input-only, fr-hidden-ignored). The four pins in `ee76e5b` kill them.

**The brief's three, on the fix round's final code:**

| Mutant | Literal edit | Killed by assertion |
|---|---|---|
| M10 | in `watch_windows()`, `vim.schedule(keep_proportions)` deleted | 7 cases: L4 split-and-close {`split`, `vsplit`, `topleft vnew`, `vertical botright new`}, L4 with the file column, L10 {`equalalways`, `noequalalways`} |
| M11@fr | in `redirect()`, `vim.api.nvim_win_set_buf(window, state.buffers[role])` deleted. This is the packet round's M11 on the changed line. | 17 cases: the L7 own-buffer, empty-Input, `:buffer` and quickfix, and line-3 cases; the typed-keys case; the four A9 cases; `focus(input)`; the restart case; the file-after-`:bdelete` case; and "opens in a new column … from `{ claude }`" (`{0,0}` vs `{0,27}`), which the list omitted until the correction (re-measure finding 7). 11 more crashed. |
| M12@fr | in `redirect()`, `local column_window = file_column_windows()[1]` → `local column_window = nil`. This is the packet round's M12 on the renamed line. | 7 cases: the L9 reuse case, the split-in-two case, the two full-width cases, the `'hidden'` and `'nohidden'` same-file cases, and the F7 case |

M10, M11@fr and M12@fr were each run three times at `ee76e5b`. Their kill sets were identical each time: SHA-1 `07c82a4b`, `8fe3a64e` and `3fcb97d7`, compared by the scratch script `t3-killsets.py`. The test-integrity reviewer measured the same kind of agreement for the packet round's head.

**The 5 survivors:**
- **i9-redirect-two-hops and i9-vimresized-scheduled** are equivalent for reads made by request. The reviewer separated them within one request (their S5 and S2).
- **i9-share-truncated** falls within the one-cell tolerance (their S1).
- **m-enter-claude** has been equivalent since this round: `build()` now ends by entering Input's window. Its kill passed to m-no-enter-input@fr, which the float and file-in-current-window cases kill.
- **m-any-window@fr** is equivalent: `redirect()` checks again, when the move runs, that the window is one of the layout's.

**Corrected (re-measure finding 5): these last two are not equivalent.** The first three hold as qualified.
- **m-enter-claude:** entering Claude's terminal window fires a user's `BufEnter` there. Under `autocmd BufEnter term://* startinsert`, `open()` leaves Input in Insert mode, `"i"` vs `"n"`. The correction adopted this separator, E2, as a test that kills the mutant.
- **m-any-window@fr:** a window that is not the layout's when `BufWinEnter` runs can become Input's through `open()`, then show the file again under `noautocmd`; the mutant's stale move then takes the file, 1 vs 5. This separator, E3, is contrived, since it needs `noautocmd`. It is reachable through the public API, and the correction adopted it as a test that kills the mutant.
- The fix round's "5 equivalent or within tolerance" (commit `977d727`) therefore holds for three of the five.

**Corrected wording (re-measure finding 8).** For the run at `7ec4647`, commit `ee76e5b`'s message says "108 were run, 79 killed by assertion, 20 not run". It means 108 rows, of which 88 were run (79 killed, 9 survived) and 20 were not.

The full table, every literal edit with its result, is in PR #9's body.

### Correction

The correction's mutants ran at `5e52956`, whose code and tests the final head carries; commits after it change only this note. Each ran one at a time, from a pristine copy of the committed tree, against the five layout files.
- Earlier runs of the set found what the last two pins close: at `818cc26`, r9-past-claude-always-faithful survived; at `db3d7ca`, three of the fix round's rows did.

**35 mutants: 31 killed by assertion, 4 survived with a reason, 0 not run, 0 killed only by a crash.** A null control, a docstring edit, survived.
- **The re-measure's 9, against the final code.** Three of them were adapted where the correction changed their text (`@c`):
  - r9-can-leave-hidden-only@c, the whole `can_leave()` return replaced by `return vim.o.hidden`;
  - r9-split-below@c, `{ split = 'above', win = window }` → `'below'`;
  - r9-no-reopen-when-input-new@c, in `open()`, `create_buf(false, true)` → `create_buf(true, true)`.
  - Two of the 9 were rebuilt from the re-measure's descriptions, since its report gives no literal text for them: r9-no-tab-switch-keep-cursor and r9-right-bound-report-always.
  - Seven are killed. r9-right-bound-report-always survives: it is equivalent while the Report and Input share a column, and only moving one of them out (re-measure finding 9, a limit) separates them. r9-no-reopen-when-input-new@c survives as equivalent, since `make_scratch()` unlists the buffer (the re-measure's verdict).
- **The wave plan's three:**
  - M10: 7 cases, the fix round's seven;
  - M11@fr: 18 cases, the fix round's 17 and P9, plus 20 crashes;
  - M12@c (`local column_window = window_taking_files()` → `nil`): 14 cases, the fix round's 7 and P1, P2, the "above it" case, the shown-twice case, the no-room case and both `hide` cases.
  - The counts were the same at `818cc26`, `db3d7ca` and `5e52956`.
- **One or more per item that changes code:**
  - item 1: c-take-any-column-window (4), c-take-layout-window (P9), c-take-non-file (P7 ×2), c-take-winfixbuf (P8);
  - item 2: c-leave-modified-ignored (P2), c-leave-shown-twice-ignored (shown twice), c-leave-hide-ignored (`hide` ×2), c-leave-hidden-ignores-bufhidden (P6 ×3), c-no-room-raises, c-no-room-no-warning and c-no-room-new-column (the no-room case);
  - item 3: c-named-input-ignored-on-build (P5), c-named-input-ignored-on-restore (the restore path).
  - c-no-room-swallows-other-errors survives. It deletes the rethrow of errors other than E36. The only other error known to reach that split is finding 11's E11 in the command-line window, a limit. Pinning the rethrow would pin that limit as behaviour.
- **Items 4 and 5's killers:** fr-restore-in-current-tab (4 cases, among them the tabs case by id), m-enter-claude (E2) and m-any-window@fr (E3).
- **The fix round's rows the correction touched.**
  - As literal edits: fr-right-from-input-only and m-second-input@fr are killed by their new pins. fr-split-same-file survives as equivalent. It drops the `~= file` check before `can_leave()`, but the redirect runs only while the aineo window still shows the file, and the column window is never a layout window. So the file is shown in two windows, and `can_leave()` is true whenever that check would have mattered.
  - Adapted where the correction changed the text (`@c`): i9-validate-last@c (6 cases), fr-no-new-input@c (3) and fr-hidden-ignored@c (the `(vim.o.hidden and bufhidden == '')` clause deleted; 1). All three are killed.

The full table, every literal edit with its result, is in PR #9's body under *Correction*.

**The fix round's 108 rows, re-run at `5e52956`** as their literal edits from `t3-fr-final.json`: 80 killed by assertion, 4 survived, 24 not run, 0 killed only by a crash.
- The survivors are the three the fix round qualified (i9-redirect-two-hops, i9-vimresized-scheduled, i9-share-truncated) and fr-split-same-file, equivalent as above.
- m-enter-claude and m-any-window@fr, survivors at `ee76e5b`, are killed, by E2 and E3.
- Of the 24 not run, 20 are the fix round's own. The other four are texts this correction changed: i9-validate-last@fr, M12@fr, fr-no-new-input and fr-hidden-ignored. Each has an adapted `@c` edit in the correction's set, and all four are killed.

## Limits, recorded

Each limit comes with its reproduction.
- **A5.** A file that reaches an aineo window without a `BufWinEnter` is not redirected.
  - A non-nested `User` autocommand of another plugin running `:edit`.
  - A scratch buffer in Input turned into a file with `:setlocal buftype=` and `:file notes.md`.
  - The attack reproduced both in its scenarios s07 Z and s11 F4/F4b. Its `SafeState` sweep was not adopted (decision 5).
- **A6.** New windows opened from aineo windows are left to Neovim; see the MVP list.
- **A10.** While another window is open, a pinned window can shrink. Sizes come back when that window closes, or with `\o`.
  - `:help` with a user's `wincmd L`: Claude drops to 1 column.
  - `:vertical botright help`: Claude drops to 20.
  - `:topleft vnew`: the right column drops to 18.
  - `:copen`: the Report drops to 3 rows.
- **A12.** The scheduled move runs after typeahead; see LR1.
- **A13.** `:bnext` from Input shows Claude's terminal there, when T4's buffer is listed. This depends on T4.
- **A bare `:edit` in Input empties it.** `u` brings the text back, and Input stays unlisted (pinned).
- **Input's own window closed after `:split` in Input** (the test-integrity reviewer's cross-note). Measured this round:
  - the remaining copy is not an aineo window, so `:edit` there is not redirected;
  - `\o` reopens Input below the Report, and the file shown in the copy stays in the right column;
  - the output is in the scratch file `t3-spike/s7-output.txt`.
- **Re-measure finding 9: the layout's own windows moved, or Claude's column split.** `:wincmd H`, `J`, `K` or `L` on an aineo window, or `:split` in Claude's, leaves no row holding the three windows in their places. Every redirect then adds a column, and `\o` does not repair it. The re-measure measured this, in `remeasure9-s01.lua`, and got the same trees at `6e9aba0`:
  - `:wincmd L` in Claude, then three redirects: `row[col[REPORT INPUT] CLAUDE c.txt b.txt a.txt]`;
  - `:wincmd K` in Input: `col[INPUT row[CLAUDE c.txt b.txt a.txt REPORT]]`;
  - `:wincmd J` or `:wincmd H` in the Report: the columns multiply the same way;
  - `:split` in Claude: `row[col[CLAUDE row[CLAUDE c.txt b.txt a.txt]] col[…]]`. This is the packet round's "untested: a split of Claude's own column", now measured.
  - These keep a single file column: `:wincmd H` in Input with Claude open, `:vsplit` in Input, `:vsplit` in the Report, and a vertical split inside the file column.
- **Re-measure finding 10: `nvim_win_set_config` moves Claude's window to another tab**, keeping its id. This was the packet round's "untested: windows moved between tabs by `nvim_win_set_config`", now measured. After `nvim_win_set_config(claude_window, { split = 'left', win = <a window of tab 2> })`, a file opened from Input in tab 1 gives `tab 2: row[CLAUDE a.txt]`. The cursor is pulled to tab 2, and `\o` goes there and changes nothing. The fix round's argument that "the layout's windows cannot leave their tab" holds for `<C-w>T`, which gives new ids, but not for this API. The same happens at `6e9aba0`.
- **Re-measure finding 11: `:e a.txt<CR>q:` typed as one input raises a raw E11.** The scheduled move runs in the command-line window: `…init.lua: E11: Invalid in command-line window`. a.txt stays in Input once `q:` closes. The same happens at `6e9aba0`. The correction's E36 catch does not cover it, since only E36 is caught. The correction measured the raw E11 on its own code in the scratch spike `t3c-spike/test_spike_e11.lua`: once with no file column open, and once above a changed file under `'nohidden'`, where the catch raises it again.

## Task lines

The wave holds the marks. The T3 row of the plan's *Implementation plan* table should read:

`| T3 | Layout (C2) and the file column with its redirect (C9) | T1 | done — PR #9, wave 2 |`

## Commits

*Recorded after the merge.* Hashes change on rebase.

## Open threads

- **For T7:**
  - pass `report_height` from `aineo.config`;
  - hand `open` and `focus` the same arrangement, built from the Claude and Report homes each time;
  - after a restart, call `open` with the new Claude buffer.
- **For the MVP review:** the list under *Readings for the MVP review*.
- **Scratch files are not durable.** The scratch paths named in this note hold this session's raw outputs. PR #9's body is the durable record of the mutant table and the findings.
- **For the orchestrator, from the re-measure's notes to the other dimensions:**
  - The modularity skill's deep-require check says it "prints nothing", but it now prints the in-home line `lua/aineo/layout/init.lua:7` on every run. It needs an in-home exclusion to stay a mechanical gate. That is an `ai/` change.
  - PR #9's *Findings* table labels the records findings `R1`–`R10`, the letter the fix round retired for readings.
- **For T5 and T7:** after a restored session, the Report's stand-in meets the same E95 that Input did (re-measure finding 3); that belongs to RP3.
